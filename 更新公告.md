# 🐺 狼人杀在线版 · 更新公告

> 当前版本：**1.7.16**（V4.2 规划层替换：HiCVN MLP 集成价值模型默认启用（AUC 0.8055 vs V3.1 0.7819，配对终裁 16/16 无劣化，σ 分桶单调列 V4.3/块1）；感知层 vote-v2 默认启用（分层 AdaBoost + 三级回退链）；PRESET_TAG 16 标签修复 + pair-v4v31 配对工具；更新公告见`更新公告.md`）（实验室多进程并发 + 第二部分：流式统计/配置矩阵/CONCURRENCY_PER_WORKER；更新公告见`更新公告.md`）（规则补足终版：丘比特判定表/自连/查验翻牌口径分离/摄梦人免疫消耗/警长平票PK/胜利条件阵营归属；更新公告见`更新公告.md`）（A 系列：公网稳定性三件套+可观测性 / 真实反馈修复：全灭终局兜底+好人 bot 发言+投票不确定性 / 快照迁移 data/ / 动态发言 / confidence 置信度接口）（人机狼恋人逻辑：护恋人/引导狼队/白天辩护）（声音设置面板：分项开关 + 上帝配音并入）（起始页精简）（系统性代码审查修复：stepText 作用域回归 / 人机公平化 / 竞选投票修复 / 事件流补齐 / 版本串同步检查）（审查修复 + 不变式自检回滚）（后台通知 + 上帝配音 + 事件流 + 平衡实验室）（快照恢复修复）（跨局记忆治理 + 房间快照恢复 + 内存看门狗 + 防滥用限流）（快速隧道代码侧加固：SW 非 2xx 回退 + 进行中房间长 TTL + keep-alive）
>
> 更新日期：2026-08-07

---

## 📜 版本总览

> 表格按「最新版本在前」倒序排列；点击版本号可查看该版本 Git 标签（https://github.com/Bingtang1019/werewolf-online/tree/标签名，相邻版本 diff 用 https://github.com/Bingtang1019/werewolf-online/compare/旧版...新版）。v1.0.0-rc.1–rc.5 为初始提交前的早期迭代，无独立提交/标签。

### 正式版（v1.0.0 及以后）

| 版本 | 日期 | 更新原因 | 更新内容 | 修改方法 | 修改收益 |
|---|---|---|---|---|---|
| [v1.7.16](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.16) | 2026-08-07 | V4.2 规划层替换 + 感知层 vote-v2 默认启用 | V4.2 HiCVN 价值模型（MLP 集成×4 + stratify 配置等权 + config-cond 16 配置 + info 信息特征）默认启用：等权 AUC 0.8055（vs V3.1 同集 0.7819，gate +2.37pp）、信息特征边际 +2.55pp、配对终裁 16/16 无劣化（CI 全含 0）；σ 分桶单调 FAIL 按 A 裁决列入 V4.3/块1 多样性增强（观察期补，不影响 ΔV 消费）；V3.1 保留回滚（VALUE_MODEL=v3）；vote-v2（分层 AdaBoost，压缩 268KB）默认启用（VOTE_MODEL_MODE 默认 v2，三级回退链 v2→v1+iso→v1→heuristic）；PRESET_TAG 16 标签修复（7 配置漏训根因）；pair-v4v31 配对工具 + 模型卡归档 archive/value-v42/ | 替换 server/ai/rollout.js + value-model-v4.js + model-loader.js + bot-brain.js + models/value-hicvn-v42.json + 新增 tools/ai/pair-v4v31.js + archive/value-v42/ | 规划层/感知层默认模型均为当前最优（V4.2 AUC +2.37pp 且 16/16 配对无劣化；vote-v2 top-1 64.7% 优于 v1 原始 60.0%）；回滚路径 VALUE_MODEL=v3 / VOTE_MODEL_MODE=adaboost 一键生效 |
| [v1.7.14](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.14) | 2026-08-07 | 生产路由修正 | configKey cap fallback（生产真人局无 preset → '12p' 等路由 cap 聚合 local）；A-2 双轨化（lab 抛错/生产显式降级）；生效范围修正（生产从未启用 V 模型体系——PAYOFF_MODE 未赋值，v1.7.4-1.7.13 共同现状）+ 启用 value 前置验收清单（12p/9p 配对） | bot-brain.js buildVoteWorld + rollout.js valuePayoffV3 + README | 生产未来启用 value 模式时路由可命中（cap 级已有 AUC 验收）；A-2 纪律与降级路径不再矛盾 |
| [v1.7.13](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.13) | 2026-08-07 | 审查 P0 修复 | 12p/9p（cap 级路由）补 AUC/校准/payoffScale（11 键对齐，12p 0.837 / 9p 0.762）；payoff() 未知 key 抛错（A-2 兜底）；4p 权重 ±34 确认特征共线非污染（R 恒=1）；meta 补 filterNotes/uncertaintyNote | fit-value-v3 cap 级评估段 + value-model.js payoff 抛错 | 验收范围 = 生产路由键全覆盖；静默 fallback 清零 |
| [v1.7.12](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.12) | 2026-08-07 | v1 反向过时（AUC 0.325）→ v3 分层 LSTD 替换 | 分层 LSTD 价值模型（TD 目标对齐 rollout 语义）· 9 配置分层采集 16998 局 · pre-only AUC 0.838（v1 0.325 → v2 0.755 → v3 0.838，同口径）· 配对裁决 9/9 不劣化、整体轻微好人倾斜（均值 −1.0pp）、12b 显著偏好人待产品确认 · A-2 启动断言（11 键）· PAV 校准附录 | 新增 server/ai/value-model.js + fit-value-v3.js；rollout 切换 v3（VALUE_MODEL=v2 回滚）；pool 自动 presetKey 路由 | 模型质量更优（+0.083）+ 胜率中性；回滚路径 VALUE_MODEL=v2 一键生效 |
| [v1.7.11](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.11) | 2026-08-07 | 第三方平衡功能（vGood接入，默认关）+ simulate档位摸底 | ③simulate已是生产可选档（UI四档），生产simulate（解析payoff）实测wolf75.5%严重偏狼→B1-9立项依据成立；⑤vGood（value-v2好人胜率）接入第三方投票（kingmaker动态站边），确定性权重混合（无RNG分叉），实测启用后局四wolf52.9→45.8（Δ-7.13pp利好好人，生产落带被破坏）→默认关闭（THIRD_BALANCE=1启用），回调待续；bot-brain value模型路径bug修复（..多一层→从未加载，vGood恒fail-open） | 替换bot-brain.js（third投票分支+thirdBalanceV+env门控/T/bias参数） | 第三方平衡功能就绪（默认关零影响）；simulate档75.5%偏狼暴露为生产问题 |
| [v1.7.10](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.10) |2026-08-07 |**热补丁**：M3.5 解绑激活验证（bot 解绑策略 + A/B 三组注入）；exilePlayer 豁免流程断裂致 lab 虚拟时钟卡死 |bot 解绑策略（人狼恋好恋人丘比特死后解绑 / 狼恋人 50% 恋人刀反制）+ A/B 注入基建（loverTest: cupid-dead-n1 / cupid-immortal + loverLocked——解绑单独效应 −3.50pp 利好好人，验收 PASS）+ exilePlayer 豁免流程链修复（放逐无效入夜，消除卡死）+ Δwolf 符号修正（v1.7.9 统计口径）+ lover-regress stdio 回显 |替换 game.js / loverCore.js / favens（goodLover·wolfLover·index）/ test/lab 基建 + 新增 T11 |解绑机制实测激活（bot 局休眠 → A/B 注入解锁）；虚拟时钟卡死消除（冒烟 5.5s/20 局 0 timeout）；统计口径修正（Δwolf = wolf_v2 − wolf_ctl） |
| [v1.7.9](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.9) |2026-08-07 |favens v1 系统性偏狼不可调参收敛（bot 行为注入层无代价端，β 75000 局实证） |①**seed 池配对方法论**：lab pool 场景（固定池 100×100 同 seed 配对）+ pool-report（Δ 与配对 CI/McNemar/组级 t，噪声带收窄至 ±1pp，1.2pp 旋钮可判定）；②**并行测试执行器** tools/parallel-tests.js（进程隔离+快照独占+超时/重试+失败 dump，全量 352s→103s，47/47）；③**c1 最终形态**（复用丘比特+狼恋人弃票+好恋人 off+红线 off）：局四 53.23/局八 47.97/十五人 48.00 三配置落带；④**favens v2 恋人权能引擎化**：loverCore.js（解绑（丘比特死后解锁）/权能槽（守护/复仇）/恋人刀/付费护短）+ loverMode 三态（off/classic/v2）+ favens v2 策略（普通狼刀法删模型继承/保丘比特）+ 真人 UI（解绑按钮/权能二选一）——**机制带代价端后 Δwolf ≤2pp（机制中性），局四 v2 落带 51.57%**；⑤第三方胜率 155000 局恒 0%（edge 屠边，β3 结题） |替换 game.js/bot-brain.js/public/+ 新增 loverCore.js/test/check-lover-v2.js/test/lab/scenarios/pool.js/stats/pool-report.js/tools/parallel-tests.js + archive/lover-v2/ |favens 结论完整（纯增益注入系统性偏狼的机制根源确认）；v2 机制自洽可演进（classic 零破坏）；测试提速 3.4 倍；全量 47/47 |
| [v1.7.8](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.8) |2026-08-06 |β 线：恋人机制 AI 对抗实验（favens 模块） |favens 六模块（cupid 三路站队/wolfLover 刀神继承+红线/goodLover 护短/conditionOn 概率条件化） + FAVENS 路由 + β1 调参诊断：完整 favens 狼 57.9% 超标，系统性偏狼 +5.7pp（丘比特 third 搅局 +3.1pp/狼恋人跟票 +1.2pp/好恋人护短 +0.8pp，归因全配对 CI 实测） |新增 favens/ + bot-brain 路由 |确认"行为注入层无代价端 → 系统性偏狼不可调参收敛"（为 v2 引擎化提供依据） |
| [v1.7.7](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.7) |2026-08-06 |α9 狼侧对称训练冻结（β 对照组基线） |刀神分类器（wolf-god-v1，AUC0.89）+ 穿衣服参数（claimGod 逐配置显式开启）+ 狼美局基线 49.6%（双 seed 复现 CI±1.3pp）+ α9 四件套存档（archive/） |wolfTrain/ + tools/ai/ + models/ |α9 基线冻结，β 对照组口径可复现 |
| [v1.7.6](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.6) |2026-08-05 |进程内单例（clock/global.rng）锁死单进程并发 |①mpool.js master/worker 对局池（fork，跨进程独立 clock/RNG）；②统一 planTasks/report 任务源（单/多进程共用）；③--workers=N 多进程分支 + vote 样本按 pid 分文件后同步合并；④deterministic/paired 两遍并行（原 Promise.all 串房风险消除）；⑤worker 自设虚拟环境 + seed 决定论重试幂等 |新增 test/lab/core/mpool.js + 改造 lab.js/config/4 scenarios |万级/配置矩阵线性加速；同 seed 跨进程 2000/2000 一致 |
| [v1.7.5](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.5) |2026-08-05 |旧规则不清晰（自连一律第三方/查验与翻牌同口径/警长平票无PK） |①丘比特阵营判定表（首轮=好人、重选=当前阵营；好+好→好人·丘比特计神职，狼+狼→狼人，好+狼→第三方，第三方+任意→第三方；指认前死亡按无阵营）②查验按阵营/翻牌按职业两口径分离（第三方狼恋人查验『好』翻牌『狼人』）③摄梦人免疫夜间伤害（狼刀/毒药无效但消耗）④警长竞选平票适用 PK⑤checkwin 阵营归属（丘比特属狼人计狼人成员）⑥新增 check-rules-174（12 项） |替换 game.js + rules.md + 2 个旧测试适配 |规则自洽、判定可测；第三方局语义正确 |
| [v1.7.4](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.4) |2026-08-05 |rollout 正确性补洞 + 动态 payoff |①payoff 阵营分流（狼 bot 不再帮好人投狼，配对基线 40%→30% 伪效应移除）；②卖狼特判（高置信不再架空）；③狼侧世界构造修复（队友强制并入采样）；④动态 payoff（R/S/M 双线加权 + 幂律曲率）；⑤margin 相对化；⑥参数纪律：进度侧 p=1 显著（34:9，p=0.0003）保留、容错侧 q=1 不显著回退、默认 p=1,q=0 |替换 server/ai/rollout.js + bot-brain.js，重启 |simulate 档好人胜率升至 35%，模型净贡献 35pp |
| [v1.7.3](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.3) |2026-08-05 |管线防线补洞 + 置信度闭环 + 波动有界 + 上线档位验收 |①模型特征数量/名称联动校验（杜绝静默错位）；②decide/rollout 的 rng 兑底改显式 throw（确定性纪律）；③confidenceOf 内部换 Platt 派生置信度（A5-1 闭环，波动不再打飞高置信决策）；④投票波动有界（只偏移 top3）；⑤LR 注释补齐完整故事（防改回 1.4）；⑥no-info 分层改"从未被提及"定义；⑦训练器预排序缓存；⑧rollout 近视边界注释；⑨如实记录"严格平移"在投票层未保持；⑩simulate 档配对验收 p<0.0001 |替换 server/ai/* + bot-brain.js + tools/ai/*，重启 |模型精度不再被波动浪费；simulate 档好人胜率 17%→40% |
| [v1.7.2](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.2) |2026-08-05 |模型管道两真 bug + 生态内高估 + 虚拟时钟架构风险 |①voted_out LR 方向修正（放逐狼→投狼者嫌疑降 / 放逐好人→投好人者嫌疑升，温和强度 0.7/1.4）；②投票特征时点修复（votes_against 排除自己票 / bot_prev_same 采集前移 / 排除竞选采样）；③A-3 分层 AUC 验收 + 跨配置验证（0.99→0.73 真实判别力，跨配置 0.7314 稳定）；④虚拟时钟并行强制 1 + 残留定时器清理 + lab fast pace env；⑤三档第三方过滤统一 / rollout 模拟跟票 / counter_seer 声称者统计 / model-loader schema 校验 |替换 bot-brain.js + game.js + server/ai/* + models/adaboost-vote-v1.json（重训）+ test/lab/*，重启 |模型验收可信（泛化 0.73 而非自举 0.99）；虚拟跑量 2000 局 74s；回归 43/43 |
| [v1.7.1](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.1) |2026-08-05 |全量回归小时级、样本管道分钟级、规则测试要摆盘 |①server/clock.js 可注入时钟单例（real/virtual，默认 real 零变化）；②game.js 37 处 Date.now/setTimeout/clearTimeout 全部换 clock.*；③debugRoom 摆盘构造器（毫秒级规则测试）；④实验室平台虚拟时间驱动（同 seed 双跑严格确定，50 局墙钟 0.52s）；⑤守卫测试 check-lab-virtual |替换 server/clock.js + game.js + test/lab/* + public/sw.js，重启 |全量回归/样本/规则测试提速约 45 倍 |
| [v1.7.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.7.0) | 2026-08-05 | 人机强度系统升级（B1）：感知层 AdaBoost + 规划层 Rollout | ①B1-8 显式 RNG 注入（全局+房间级 xorshift128+，快照续流，同种子对局逐字节一致）；②B1-1 纯行动接口拆分（decideVote/decideNightKill，态度排除）；③B1-0 基线（simulate 档 13 人局狼 96.2%）； ④B1-2 样本管道（2000 局 5.6 万条 vote 样本，决策时刻采集）；⑤B1-3 AdaBoost+Platt 训练器（三件套验收 Brier0.03/AUC0.99/avgP差0.78）；⑥B1-4 感知层注入（模型 P(wolf) 混合，好人胜率 3%→40%）； ⑦B1-5 Rollout 规划层（信念采样+模拟本轮投票，64 世界）；⑧B1-1② 阶梯重排（easy←现smart、smart←现simulate、simulate←新simulate+rollout）；⑨B1-6 配对验收（400 局 McNemar p=0.0274 显著）； ⑩B1-9 平衡验证（13 人局狼 63-84%，较基线改善未均衡） | 替换 server/ai/ + bot-brain.js + game.js + tools/ai/ + models/，重启 | 人机投票有了数据支撑的感知与前瞻；阶梯强度重构 |
| [v1.6.4](https://github.com/Bingtang1019/werewolf-online/tree/v1.6.4) | 2026-08-05 | 隧道 50% 失败率、全灭不结束、好人 bot 沉默、投票失真 | ①公网稳定性三件套：POST 幂等重试（opId 去重防“说两遍”）、失败轮询退避（已有指数退避确认）、快速隧道免 SSE；②可观测性：healthz/stats 增 HTTP 总数/失败/p95、慢请求日志、stats/debug token 防护；③requestTimeout 90s；④A2-1 全灭判平局结束 + 阶段入口兜底（终局幂等）；⑤A2-3/A2-5 好人 bot 报查验/辩解/表态 + 组合式生成（lexicon.json）；⑥A2-4 投票不确定性表达（confidence.js 置信度接口，A5）；⑦A4 动态发言次数（人类占比）；⑧A3 快照迁移 data/；新增 4 个专项测试 | 替换 server/game/bot-brain/public/sw.js，重启，Ctrl+F5 | 弱网“点不动”大减、全灭必终局、人机更像真人、观测不再靠猜 |
| [v1.6.3](https://github.com/Bingtang1019/werewolf-online/tree/v1.6.3) | 2026-08-05 | 人机狼恋人策略缺失、音效开关粒度粗、首页标签过时 | 狼恋人护恋人/狼频道引导/白天辩护；声音面板分项开关+上帝配音并入；首页去标签 | 替换 bot-brain.js + public/，重启，Ctrl+F5 | 人机恋爱局更拟真，音效可单关，首页简洁 |
| [v1.6.2](https://github.com/Bingtang1019/werewolf-online/tree/v1.6.2) | 2026-08-05 | v1.6.0 引入 stepText 作用域回归，夜晚面板崩溃 | 修复 stepText；人机公平化（去全知）；警长投票投竞选者；事件流补齐；死代码清理；POST 参数校验 | 替换 server/game/bot-brain/public，重启，Ctrl+F5 | 夜晚不再崩，人机不作弊，勘查数据完整 |
| [v1.6.1](https://github.com/Bingtang1019/werewolf-online/tree/v1.6.1) | 2026-08-05 | 狼投票方向错、狼总数先验漂移、onChange 导出失效 | 狼投票取反；getWolfCount 取配置；factionOf 第三方判定；不变式自检+快照回滚；限流防伪造 | 替换 server/game/bot-brain/public，重启 | 人机策略合理，异常房间自动回滚 |
| [v1.6.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.6.0) | 2026-08-05 | 需要通知/配音/可勘查事件流/平衡数据 | 后台通知；上帝配音；事件流 /api/debug；蒙特卡洛平衡实验室 | 替换 server+game+public/client.js，重启 | 挂机不错过回合，运维可勘查，平衡有数据 |
| [v1.5.7](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.7) | 2026-08-05 | 快照恢复到「猎人等待开枪」时白天卡死 | resumeRoom 补 hunter_shot 分支；.gitignore 补 rooms.json.bak | 替换 game.js+server.js，重启 | 断线恢复不再卡白天 |
| [v1.5.6](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.6) | 2026-08-05 | 重启丢房间、bot 跨局记忆泄漏、无防滥用 | 快照 rooms.json 持久化+启动恢复；bot 记忆重置；内存看门狗；IP 限流+房间上限 | 替换 game.js+server.js，重启 | 重启不丢局，防滥用 |
| [v1.5.5](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.5) | 2026-08-05 | 快速隧道断连时页面打不开 | PWA 非 2xx 回退缓存；房间 TTL 分阶段；keep-alive 65s | 替换 server.js+public/sw.js，重启 | 断连也能打开页面 |
| [v1.5.4](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.4) | 2026-08-05 | trycloudflare 524 重连风暴 | SSE 连续失败 3 次降级纯轮询 | 替换 public/client.js，Ctrl+F5 | 公网稳定 |
| [v1.5.3](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.3) | 2026-08-05 | 「模拟」人机按钮点击不高亮 | setBotLevel 白名单补 simulate 档 | 替换 public/client.js，Ctrl+F5 | 前端小修复 |
| [v1.5.2](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.2) | 2026-08-05 | 投票分票/卖狼/保命/选牌策略缺失 | 投票集中；卖狼美人；守卫守神职；摄梦人保命；盗贼选神；职业专属发言 | 替换 bot-brain.js，重启 | 人机策略更强 |
| [v1.5.1](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.1) | 2026-08-05 | 人机不懂第三方获胜逻辑 | factionOf 阵营认知；狼不刀第三方；第三方互保；女巫恋人必救 | 替换 bot-brain.js，重启 | 人机适配人狼恋局 |
| [v1.5.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.5.0) | 2026-08-05 | 人机发言单调、无态度模型 | simulate 档：5 状态马尔可夫态度+多证据源+Sigmoid 双校准+狼队风格 | 替换 game+bot-brain+public，重启 | 第四档人机 |
| [v1.4.4](https://github.com/Bingtang1019/werewolf-online/tree/v1.4.4) | 2026-08-05 | bot 白天只有固定话术 | 发言体系升级：主+次发言、对跳辩论、穿衣服自证、狼夜频道、遗言 | 替换 game.js+bot-brain.js，重启 | 对局更像真人 |
| [v1.4.3](https://github.com/Bingtang1019/werewolf-online/tree/v1.4.3) | 2026-08-05 | bot 无推理、无白天发言 | 银水/守人记忆；对跳查验；魅惑策略；白天发言；botDelay 负延时修复 | 替换 game.js+bot-brain.js，重启 | bot 高阶推理 |
| [v1.4.2](https://github.com/Bingtang1019/werewolf-online/tree/v1.4.2) | 2026-08-05 | 夜晚面板渲染崩溃（glow 笔误） | 渲染笔误修复；新增客户端渲染回归测试 | 替换 public/client.js，Ctrl+F5 | 夜晚不再崩 |
| [v1.4.1](https://github.com/Bingtang1019/werewolf-online/tree/v1.4.1) | 2026-08-05 | 节奏/离线/全职业回归需求 | 夜晚 45s/白天 60s；bot 延迟 10s±2.5s；离线模式；check-allroles | 替换 game+bot-brain+public，重启，Ctrl+F5 | 全职业验证+单机可玩 |
| [v1.4.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.4.0) | 2026-08-05 | 人机只有两档 | 三档人机（挂机/简单/智能贝叶斯+狼队共享），bot-brain.js 模块化 | 新增 bot-brain.js+game.js+public/，重启 | 人机有威胁 |
| [v1.3.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.3.0) | 2026-08-05 | 行动无反馈、拉人不便、复盘无话题 | 行动反馈动画；房主明细；震动；邀请链接；PWA；环境粒子；字号 | 替换 game.js+public/，Ctrl+F5 | 对局体验+社交传播 |
| [v1.2.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.2.0) | 2026-08-05 | 首页简陋 | 双卡入口；昵称生成器；房号实时校验；首屏夜景；在线房间统计 | 替换 server.js+public/，Ctrl+F5 | 首页焕新 |
| [v1.1.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.1.0) | 2026-08-05 | 状态刷新不够即时 | SSE 推送唤醒：轮询降为 30s 心跳；断线退避重连 | 替换 server.js+public/client.js+bat | 响应即时、带宽大降 |
| [v1.0.2](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.2) | 2026-08-05 | 引擎规则测试覆盖不足 | check-gaps 7 项专项（女巫自救/摄梦链/猎人毒杀/连守/全弃权/PK平票/房主转移） | 仅新增测试 | 规则有据可查 |
| [v1.0.1](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.1) | 2026-08-05 | 死者投票脏票、测试假通过 | 引擎拒绝死者投票；修复 2 处假通过断言；测试端口去重 | 替换 game.js+测试 | 规则卫生 |
| [v1.0.0](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.0) | 2026-08-04 | 正式发布 | 合并 rc.4-rc.6 迭代：UI/UX v3+Visual v1-4+音效+安全+防连点 | 替换 server/game/public/bat/package.json | 正式可用 |

### 快速迭代版（rc 系列）

| 版本 | 日期 | 更新原因 | 更新内容 | 修改方法 | 修改收益 |
|---|---|---|---|---|---|
| [v1.0.0-rc.6](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.0-rc.6) | 2026-08-04 | 空刀误发、聊天切频道不刷新、结果未转义 | 狼人空刀修复；聊天刷新；投票结果转义；夜晚遮罩启用；文档对齐 | 替换 game.js+public/ | 关键 bug 修复 |
| [v1.0.0-rc.5](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.0-rc.5) | 2026-08-04 | 路径穿越漏洞、房间无 TTL | 双层路径防御；TTL 回收；body 上限；参数校验；日志轮转 | 替换 server.js+game.js | 公网安全 |
| [v1.0.0-rc.4](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.0-rc.4) | 2026-08-04 | 狼频道白天可见等 5 项体验 | 狼频道仅夜晚；PK 过滤；夜晚禁言；丘比特可换；魅惑区隐藏 | 替换 game.js+public/client.js | 细节体验 |
| [v1.0.0-rc.3](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.0-rc.3) | 2026-08-04 | UI 凌乱、遗言框失焦 | 遗言框快照恢复；复选框样式；UI 整体整理 | 替换 public/ | 界面整洁 |
| [v1.0.0-rc.2](https://github.com/Bingtang1019/werewolf-online/tree/v1.0.0-rc.2) | 2026-08-04 | 公网卡顿崩溃、警长开关不可关 | 版本化轮询+gzip+自适应频率；警长开关；崩溃容错；自动重启 | 替换 client.js+server.js+bat | 公网稳定 |
| v1.0.0-rc.1 | —（初始提交前） | 初始版本 | 基础功能全部可用 | — | 首发可用 |

---
## 🔧 1.7.0 —— 人机强度系统升级（B1：感知层 AdaBoost + 规划层 Rollout）
- ①**B1-8 显式 RNG 注入**：server/ai/rng.js（xorshift128+）全局+房间级；快照记录 RNG 状态（恢复后序列不重演）；同种子对局 actionLog/事件流逐字节一致（deterministic 验收）
- ②**B1-1 纯行动接口拆分**（server/ai/legacy/decide.js）：decideVote/decideNightKill 纯函数（阵营分流/跟票/卖狼/平局注入 rng 打破），态度逻辑排除干净（留给 C1 混沌层）
- ③**B1-0 基线**：simulate 档 13 人局狼胜率 96.2%（500 局 records 落盘）
- ④**B1-2 样本管道**：vote 特征（server/ai/features.js，13 维公开特征，训练/推理同构）；game.js 决策时刻采集钩子；lab sample 落 2000 局 + 5.6 万条样本
- ⑤**B1-3 AdaBoost+Platt 训练器**（tools/ai/train-vote-adaboost.js）：200 决策树桩；三件套验收 Brier0.0301 / AUC0.9901 / avgPWolf-avgPGood 0.7799；模型 models/adaboost-vote-v1.json（随仓分发，fail-open 加载）
- ⑥**B1-4 感知层注入**（server/ai/model-loader.js）：好人侧投票前模型 P(wolf) 混合（0.6 信念+0.4 模型）；同 seed 100 局好人胜率 3%→40%
- ⑦**B1-5 Rollout 规划层**（server/ai/rollout.js）：信念采样 + 模拟本轮投票结算；逐候选“投 X 促成 X 放逐”收益（+2/-1 不对称）；64 世界
- ⑧**B1-1② 阶梯重排**：easy←现smart、smart←现simulate、simulate←新simulate(+rollout)（LEVEL_MAP）
- ⑨**B1-6 配对验收**：lab paired 400 局，新simulate vs 旧simulate discordant 83:56，χ²=4.86 p=0.0274 显著通过
- ⑩**B1-9 平衡验证**：13 人局狼胜率 easy63% / smart84% / simulate83%（无超时无错误）；模型对好人侧提升最大；较 96.2% 基线改善但未均衡
- **蒙特卡洛实验室架构重写**（数据生产/消费分离）：GameRecord 标准 schema（lab.game-record@1，JSONL 落盘）+ core（config/room-runner/pool/recorder/events，确定性驱动、错误分类 config/engine/stall、seed 注入、checkpoint 续跑）+ scenarios（baseline/sample/deterministic/paired）+ stats（wilson/mcnemar/report 纯函数）；依赖单向（scenario→core→game.js）；本次 B1 全程消费：基线、样本、确定性验收、配对验收、平衡验证

## 🔧 1.7.6 ——实验室多进程并发（第一部分）
①mpool.js（master/worker，child_process.fork）：跨进程各自持有独立 clock/全局 RNG → 进程级并行是唯一正确的扩容方式（进程内单例锁死 parallel=1 的根因）。协议 v1（pull 模型，每 worker 1 in-flight 天然背压；cfg 随任务下发；摘要/全量回传；checkpoint 在 master）。
②统一 planTasks/report 任务源：baseline/sample/paired/deterministic 四场景拆纯函数，单进程（runPool）与多进程（mpool）共用同一任务源。
③--workers=N（preset 默认 1，向后兼容）：lab.js 多进程分支 + vote 样本按 pid 分文件（防并发 append 交错）后同步合并（异步 createWriteStream + process.exit 曾丢数据）。
④deterministic/paired 两遍并行：a/b 两个独立任务跨进程跑（原 Promise.all 双局并跑有串房风险，已消除）；跨进程隔离下 hash 一致才是真一致。
⑤坑已处理：worker 自设虚拟环境（env + clock.setMode，fork 不继承运行时状态）；超时杀进程后 seed 决定论重跑幂等；skip（checkpoint）计入完成并触发收尾（曾死等）。
⑥第二部分（预留落地）：流式 summarize（createStreamStats，万局 O(1) 内存，streamable 场景多进程不存全量 records）；配置矩阵任务源 matrix scenario（--caps=6,8,13 --games=50 或 --matrix='8:wolf2,...;13:...'，report 按 cap 分组 Wilson CI）；CONCURRENCY_PER_WORKER（--conc=N，每 worker 多 in-flight，真实模式生效，虚拟模式强制 1 防串房）。
⑦第三方平衡摸底基建：balance scenario（预设库 16 种 + 随机配置生成器，比例 3:2；--cupid-only 只跑含丘比特预设）；修复 autoBotName 死循环（BOT_NAMES 12 个名字，≥13 bot 局（15 人局）do-while 永不退出——生产 15 人全 bot 场景同样会卡死，扩容至 20 个 + 序号兜底）。
验证：同 seed 跨进程 2000 局胜负 2000/2000 一致；deterministic 跨进程 hash 4/4；sample 多进程 vote 样本合并 24 行无残留；baseline 2000 局 w4 较 w1 约 1.65x（cap8 单局过快，进程开销主导；收益在万级/配置矩阵/单局变慢场景）。

## 🔧 1.7.5 ——规则补足终版（丘比特判定表 + 两口径分离 + 摄梦人/平票/胜利条件）
①丘比特阵营判定表（rules.md 三·新增）：首轮丘比特身份=好人、重选=当前阵营；丘比特不在情侣中=情侣组合判定，自连=丘比特+被连者组合；好+好→好人（丘比特计神职）、狼+狼→狼人、好+狼→第三方、第三方+任意→第三方；指认前死亡按无阵营（不影响屠边与胜负）。
②查验与翻牌口径分离（三.13）：查验按阵营（第三方狼恋人/第三方丘比特显示『好』，属狼人阵营丘比特显示『狼人』）；翻牌按职业（狼美人翻牌『狼人』）——两口径分离是预期行为。
③摄梦人（三.7/7b）：梦游者免疫所有夜间伤害（狼刀、毒药）；毒药/解药对梦游者无效但均消耗；带走为结算不受免疫保护。
④警长竞选平票适用 PK（三.3）：平票者 PK 再投，得票最多当选，再平票无人当选。
⑤胜利条件与 checkwin 阵营归属（四）：第三方胜=好人阵营与狼人阵营全部死亡且第三方存活（含孤身）；丘比特属狼人阵营计狼人成员（好人胜需其死亡、屠边不受影响）、属好人计神职（参与屠边）。
⑥cupidCamp 默认 null（未指定按无阵营）；checkWin 导出供测试/实验室；新增 check-rules-174（12 项）；simulate-third-survive/simulate-wolfbeauty-gun 适配新规则。

【B 口径对 bot 的已知影响（1.7.5 设计，非 bug）】①信息双轨：查验（阵营）与翻牌（职业）是独立信号源，bot 融合分开建模；②翻牌数『狼人』把狼恋人计入 → 好人 bot 狼阵营存活数系统性高估 1（方向保守，策略层 ±1 修正待评估）；③预言家 bot 查狼恋人=『好』，查人目标需加入"查第三方"预期价值；④checkwin 阵营映射直接进判定，勿在赢家判定里再查身份牌。

## 🔧 1.7.4 —— rollout 阵营分流 + 卖狼特判 + 动态 payoff

①**Q1（P1）rollout payoff 阵营分流**：此前 payoff 段无 world.faction 判断——狼 bot 也在用好人视角（放逐狼 +3），与 decideVote argmin 直接打架。修复后重跑配对基线：好人胜率 40%→30%（38:0→28:0）——狼 bot"帮好人投狼"的伪效应被移除，**83:56 / 38:0 中确含该成分**（用户预测验证）。
②**Q2（P1）卖狼特判**：rollout pool 排除队友 → 卖狼目标永远不可能是 rv.target，margin≥阈值时卖狼被静默架空——入口特判（margin=Infinity 保证不回退），与 decideVote 优先语义对齐。
③**狼侧世界构造修复**：信念采样会把队友标成好人（P(wolf)≈0.23，77% 概率）→ 狼 bot 反而倾向投队友；采样后强制并入真实队友（规则内知识，B1-7 允许）。
④**动态 payoff**（Q1 基础上）：wolfAlive/godAlive/villAlive = roleCounts − 翻牌死亡（全部公开精确）；payoffP/payoffQ 曲率（0 时精确退化为静态 3/1.5）。语义推演验证：开局 P*=0.333 → 残局狼少 0.294（更敢投）→ 神快崩 0.455（更保守）。
⑤**margin 阈值相对化**：绝对 2 相对 ±W×3 总分尺度近乎恒真（fallback 极少触发）——改为相对 0.05×W×scale。
⑥**参数纪律**（每版记录，不显著回退）：
 - p=q=1 vs 静态：不显著（31:22，p=0.27）→ 放弃组合
 - **p=1,q=0（进度侧：狼越少越敢投）vs 静态：显著（34:9，χ²=13.40，p=0.0003，好人 +6.3pp）→ 保留为默认**
 - p=0,q=1（容错侧：更保守）vs 静态：不显著（28:26，p=0.89）→ 回退
⑦最终默认 **p=1, q=0**（env 可配）；模型配对验收（simulate 档）：带模型好人 **35%** vs 无模型 **0%**（35:0，p<0.0001，模型净贡献 35pp）。
⑧**二期数据驱动 ΔV（曲率由数据定，不猜 p）**：`tools/ai/fit-value-model.js` 从 GameRecord 重建投票时刻状态 (R,S,M,N)（修复 roleKey 重建后，1500 局 4717 样本，逻辑回归 AUC=**0.7789**，系数方向全对）；payoff = V(s′)−V(s)（`models/value-vote-v1.json`，PAYOFF_MODE=value，fail-open 回退解析版）。
⑨**数据版配对验收**（400 局 × 两个 seed）：62.5%/62.3% vs 解析版 34.8%/39.0%（129:18 / 117:24，χ²=82.31/60.03，p<0.0001）——**数据版显著且稳定，保留为默认**。
⑩**上线档位（value + 模型）**：带模型好人 **63.0%** vs 无模型 16.0%（48:1，p<0.0001，模型净贡献 47pp）——simulate 档好人胜率 35%→63%。
⑪**平衡提醒**：好人 63% / 狼 37%——从"狼太强"翻转到"好人偏强"，下一阶段需平衡回调（混沌层/其他杠杆）；V 模型拟合自旧生态，上线后生态偏移需周期性重拟合（A-3 纪律）。

---

（env 可配）；模型配对验收（simulate 档）：带模型好人 **35%** vs 无模型 **0%**（35:0，p<0.0001，模型净贡献 35pp）。

---

## 🔧 1.7.3 —— 管线防线 + 置信度闭环 + 波动有界

①**P1-1 模型特征数联动校验**：model-loader 校验 `m.features.length === FEATURE_NAMES.length` 且名称逐一相等——features.js 改维度后旧模型 fail-open，杜绝"不报错不 NaN、静默用错特征"。
②**P1-2 rng 兑底改显式 throw**：rollout 的 `Date.now()` 兜底与 decide 的 `Math.random` 兜底全部改为 throw——缺 rng 是调用方 bug，让它在测试里炸出来（B1-7 P0②）。
③**F2 confidenceOf 接 Platt（A5-1 闭环）**：内部优先 `1-|2P-1|`（模型可用时），fail-open 回退 suspicion 方差——波动层与未来混沌层零改动受益；此前模型给出 P(wolf)=0.85 的高置信决策会被 suspicion 方差误判低置信（conf<0.55）打飞，paired +24pp 是"被波动浪费后"的结果。
④**F5 投票波动有界（A5-2 定稿）**：只允许偏移到分数 top3，杜绝"上头投 rank12"。
⑤**F1 LR 注释补齐完整故事**：投票方向统计（放逐狼 5.5% / 放逐好人 30.7%）、1.48 贝叶斯最优被故意放弃的级联原因、1.4→1.0 让好人胜率翻倍实测——防后人见 30.7% 就把 1.0 改回 1.4。
⑥**P2-2 no-info 分层定义修正**："从未被提及"= prev_votes=0 && checked=0 && accused=0（剔除投票早期时间偏差）。
⑦**P2-3 训练器预排序缓存**：重训 20-60s → 3-8s（重训频繁期收益明显）。
⑧**P2-4 / F9 注释**：rollout 近视边界（放逐连锁未模拟，狼美人风险被低估）、vote-direction-stats 的 pk 缺口。
⑨**F6 如实记录**："严格平移"在投票层并未保持——新 smart 投票=纯贝叶斯+模型（attitude 已排除，符合 P0③），83:56 配对证明新投票逻辑更好；文档已如实写，供后人核对清单。
⑩**上线档位配对验收（simulate）**：带模型好人 **40.0%** vs 无模型 **2.0%**（不一致对 38:0，χ²=36.03，p<0.0001，模型净贡献 38pp）；结合 F2/F5，simulate 档好人胜率从 17% 升至 **40%**。

---

## 🔧 1.7.2 —— 模型管道修复（LR 方向 / 特征时点 / 验收生态剥离 / 虚拟时钟）
- **① A-1 真 bug：voted_out LR 方向修正**：`voted_out_wolf 1.2→0.7`（放逐狼→投他的人≈全是好人→嫌疑应降）、`voted_out_good 0.8→1.4`（放逐好人→投他的人混着狼→嫌疑应升）——方向修正（自 v1.5.0 就存在，B1-1 后原样继承），强度取温和值（实测本生态好人胜率变化诚实记录：旧自举模型 40%→26%，主因是模型真实化 + 放逐好人时嫌疑升高惩罚被带偏的多数好人）。
- **② A-2 真 bug：投票特征训练/推理时点不一致**：`votes_against/vote_lead` 训练时含自己刚投的票（推理时不含）→ 统一排除自己票；`bot_prev_same` 训练时恒 1（actionLog 先 push 后采集）→ 采集移到 push 之前；B-4：仅 `phase==='vote'` 采集，排除竞选/平票投票的 day1 噪声样本。
- **③ A-3 结构性：离线指标生态内高估实证**——修复 A-2 后重训：全样本 AUC 0.9901→**0.7362**（旧 0.99 是 votes_against 自举 + bot_prev_same 恒 1 的假象）；**无查杀无票信息 AUC=0.6466**（真泛化核心）、有查杀信息 AUC=0.8161（跟随公开查验，合法信息流）；**跨配置验证（带模型 bot 新采 500 局）全样本 0.7314 / 无信息 0.6738**——与训练几乎一致，生态内过拟合实锤排除；验收门槛更新（Brier<0.22 + 全样本 AUC>0.6 + 无信息 AUC>0.6 + 有查杀 AUC>0.6，avgP 差值降为报告项——旧 0.2 门槛对应自举假模型）。
- **④ A-4 架构风险：虚拟时钟**——并发强制 parallel=1（clock 单例共享队列/now，交叉 tickNext 串房）；`clearAll()` 每局 finally 清残留定时器（防队列线性膨胀 O(n) 插入退化）；lab.js 内置 fast pace env（BOT_DELAY_MS=100/PHASE_TIMEOUT=30/NIGHT_TIMEOUT=20/CHAT_INTERVAL=0）。
- **⑤ B 类**：三档第三方过滤统一（easy/smart 补 `faction==='third'` 不投自己阵营，与 simulate 一致）；rollout 模拟投票加跟票集中（与现实 decideVote 一致，平票边缘 payoff 不失真）；counter_seer 只统计"我是预言家/我跳预言家/跳预"声称者（"预言家快出来带队"不再误报）；B-5 检查：新模型 platt.A=1.19 适中、正例均值 0.456 不饱和，0.4 混合权重保留。
- **⑥ C 类**：model-loader 加载后 schema 校验（stump f 越界/platt 非有限 → fail-open）；vote_lead O(n²)→O(n)；环距离归一化分母 total/2（值域 0..1）。
- **⑦ 模型重训**：2000 局新样本（虚拟模式 74s）→ 新模型（Brier 0.1885 / AUC 0.7362 / 无信息 0.6466 / 有查杀 0.8161）。
    - **⑧ 对照实验与配对验收（A-1 混杂归因厘清 + B1-4 真正验收）**：
      - 投票方向统计（200 局）：放逐狼 → 投狼者中狼比例 **5.5%**（<<23% 先验，狼不投狼实锤）→ `voted_out_wolf:0.7` 方向实锤保留；放逐好人 → 投好人者中狼比例 **30.7%**（仅高先验 7.7 点，70% 是投错的好人）→ 升嫌疑是低置信信号 → `voted_out_good` 由 1.4 调回 **1.0 中性**（不对称做彻底——1.4 在好人常投错的生态里惩罚多数好人→互投→狼渔利）。
      - 同 seed 100 局对照：带模型好人 **48%** vs 无模型（LAB_NO_MODEL）**24%** → **模型净贡献 +24pp 实锤**（B1-4 模型有效在真实化后重新立论）；且 `voted_out_good 1.4→1.0` 让好人胜率 24%→48% 翻倍。
      - **模型配对验收（McNemar，100 局同 seed 逐局）**：带模型 49% vs 无模型 21%，不一致对 32:4，**χ²=20.25，p<0.0001 → 显著，模型有效**——护栏门槛（分层 AUC）管"模型是不是假的"，配对检验管"模型有没有用"，两个都要。
    - **⑨ rollout 两处**：得分差距 margin<2 回退 decideVote 跟票目标（低信息局 64 世界采样噪声大，防与公众票型冲突分票）；payoff 含身份揭示价值（放逐狼 +3 / 误放逐好人 -1.5——放逐狼=票型变强证据喂给 A-1 票型推理，缓解过度风险厌恶）。
    - **⑩ 特征③ checked_by_trusted**（被 credibility>=0.6 的可信预言家声称者查杀）：无查杀无票信息 AUC **0.6466→0.7074**（泛化核心 +6pp）；模型重训（14 维，0.7230 / 无信息 0.7074 / 有查杀 0.6941）。
    - **⑪ flaky 根因修复**：check-bot-advanced 投票平局/波动走房间 rng，server 无 SEED → 断言偶发失败——spawn 注入固定 SEED，3/3 稳定，全量 43/43。
    - **⑫ 反直觉数字排查（有查杀 AUC 0.8161→0.6941 < 无信息 0.7074）**：三组分层（纯 checked_wolf / 纯 checked_by_trusted / 两者）验证——**checked_by_trusted 特征在全部训练样本恒 0**（好人 bot 的 seerClaims credibility 恒 0.5：updateSeerClaims 仅校准狼 bot——公平性修正，采集仅好人 bot → 特征死代码）→ **弃用回 13 维**（0.7230/0.7074/0.6941 指标不变）；0.6941 为真实水平（旧 0.8161 含 votes_against 共振自举，A-2 修复后消失），"有查杀<无信息"解释为 bot 生态狼悍跳查杀好人稀释判别力——**接受并归档**；"用公开放逐/死亡对齐校准好人 credibility"留作 1.7.3 候选。
    - **⑬ 发布动作**：model-validate（配对验收）、vote-direction-stats（投票方向）、eval-vote-auc（跨配置 AUC）三个正式工具已入 README 测试清单（--quick 可跑 50 局）。




## 🔧 1.7.1 —— 可注入时钟 + 虚拟时间加速
- **① server/clock.js 可注入时钟单例**：`setMode('real')` 生产（行为零变化，默认）/ `setMode('virtual')` 测试与实验室（墙钟不随游戏流逝）；定时器按触发时刻有序排队，`tickNext()` 同步执行回调。
- **② game.js 37 处定时器/时间戳全部换 clock.***：阶段超时、夜晚步骤、猎人/盗贼倒计时、bot 调度、发言限流、消息/事件时间戳、快照恢复重挂；bot-brain.js 盲区自查通过（决策纯同步、随机全走 rng()、无真实时间依赖，虚拟加速不破功）。
- **③ debugRoom 测试构造器**：跳过建房/发牌直接摆盘（phase/roles/night 自动补齐前置步骤），seed 可确定性，毫秒级规则测试；已导出供测试/实验室使用。
- **④ 实验室平台升级虚拟时间驱动**（runOneLabGame）：同 seed 双跑严格确定（事件流逐字节一致）；驱动只推时钟游戏自动走完——**50 局墙钟 0.52s**（真实模式每局 2-4s），2000 局样本管道从分钟级压到秒级。
- **⑤ 守卫测试 check-lab-virtual**：墙钟 <3s + 同 seed 两遍事件流 hash 一致（防架构回归）。


## 🧪 蒙特卡洛实验室平台（架构重写：数据生产/数据消费分离）

**为什么重写**：实验室曾是单文件工具脚本（check-balance-lab.js），结果只进 console 无法复用/对比；而它接下来要同时服务 A2（平衡验证）、B1（样本管道/配对验收/确定性验证）、C1（混沌层 A/B）三条线——每条线要的“数据形态”不同，但“跑局”这件事是同一件。核心动作只有一个：**定义 GameRecord 标准数据模型，把“跑局”和“分析”彻底解耦**——跑一次局，胜率报告、训练样本、配对统计、确定性重放全都能从中产出。

**依赖方向（单向）**：`scenario → core → game.js`；stats 谁都不依赖。core 不 import stats，scenario 不碰 game.js。

**GameRecord schema（架构锚点，lab.game-record@1，JSONL 落盘）**：
- `gameId/seed/scenario`：由 seed+局号派生，全局唯一（配对/确定性模式的核心）
- `config`：cap/counts/botLine/winMode/tieRule——跑局参数可复现
- `result`：winner + timeout + **error.kind 错误分类（config/engine/stall）**——config 错是实验室问题、engine 错是 game.js 问题、stall 是驱动问题，三者在报告里分开计，一跑就知道该查谁
- `players`：ground truth（训练标签来源）；`events`：标准化事件流（样本管道/重放地基）；`firstKill`：首刀分布

**core/（跑局）**：
- `config.js`：默认值 < preset < CLI 三级合并 + 校验；狼数阶梯（13 人=3 狼，与 B1-3 验收先验对齐）；默认 botLine；`--counts=wolf2,seer1,...` 覆盖；`--sample-file/--out` 相对项目根解析
- `room-runner.js`：单局执行器——**确定性驱动**（等 bot 行动/投票/发言达配额再推进，消除调度竞态，deterministic 验收的前提）；错误分类；**B1-8 seed 注入**（每局重置全局 RNG → 房间派生一致）；labSampleFile 投票样本采集（game.js 决策时刻钩子）
- `pool.js`：并发池 + seed 派生（`${base}-${i}`）+ checkpoint 续跑（doneSet 跳过已完成 gameId，2000 局中断不废）
- `recorder.js`：流式 JSONL 落盘 + gameId 去重
- `events.js`：事件标准化（宽松提取 + 透传，映射表按 game.js 实际事件结构核对后固化）

**scenarios/（分析，各 30~40 行）**：baseline（胜率 Wilson CI + 首刀 + 错误分类，`--out` 落盘）、sample（records + vote 样本双落盘）、deterministic（同 seed 双跑 → 事件流 id→座位归一化后 sha256 对比）、paired（同 seed 双策略 → McNemar 配对检验）。

**stats/（纯函数，零依赖，可单测）**：wilson（95% CI）、mcnemar（连续性校正 + erfc）、report（records → 报告对象）——`check-lab-stats.js` 专项单测。

**使用**：
```bash
node test/lab/lab.js smoke
node test/lab/lab.js baseline --games=500 --cap=13 --parallel=8 --bots=simulate
node test/lab/lab.js sample --games=2000 --cap=13 --out=data/lab-records.jsonl --sample-file=data/vote-samples.jsonl
node test/lab/lab.js deterministic --games=20 --seed=abc
node test/lab/lab.js paired --strategy-a=simulate --strategy-b=smart --games=400 --seed=pair-001
```

**验收标准**：任何新实验 = 新增一个 30 行的 scenario 文件，core 一行不改；红线：不做 GUI/看板、不引数据库、不搞插件系统（scenario 就是函数，注册表就是对象）、stats 不引依赖。

**B1 全程消费**：基线（simulate 档 13 人局狼 96.2%，500 局落盘）、样本（2000 局 5.6 万条 vote 样本）、确定性验收（同 seed 事件流 hash 逐字节一致）、配对验收（400 局 McNemar p=0.0274）、平衡验证（各档胜率/首刀/局时）。

## 🔧 1.6.4 —— A 系列实施（公网稳定性 + 真实反馈 + 基建）

> 来源：08-05 真实公网测试（cloudflared metrics 证实 trycloudflare 50% 请求失败）+ 真实玩家反馈；工作清单 A 系列。

### ① 公网稳定性三件套（A1）
- **P1-1 POST 幂等重试**：act()/chatSend() 网络失败自动重试（幂等动作清单），携带 opId；服务端写操作统一入口（action/chat/advance）recentOps 去重——先写 pending 占位再处理（并发窗口堵死“发言说两遍”），只缓存轻量确认不缓存大响应，懒清理无定时器，无 opId 旧客户端放行
- **P1-2 轮询退避**：现有指数退避（连续失败 2s/4s/8s…15s 封顶，成功即恢复）确认已满足，无需改动
- **P1-3 快速隧道免 SSE**：*.trycloudflare.com 检测 → 直接纯轮询，省掉每次进房的 3 次 SSE 失败风暴
### ② 可观测性（A1-P2）
- healthz、/api/stats 新增 http.total/fail/p95ms（固定 20 桶直方图，O(1) 估算）；慢请求日志（>500ms，≥1s 节流）
- /api/stats、/api/debug 访问控制：STATS_TOKEN/DEBUG_TOKEN（Authorization: Bearer 或 X-API-Token，不放 query）；未配置仅绑 localhost（防裸奔）
- **P2-2**：requestTimeout=90s（请求生命周期）；headersTimeout=70s > keepAliveTimeout=65s 已确认满足
### ③ 真实玩家反馈（A2）
- **A2-1【严重】全灭不结束**：根因 = checkWin 对“无活人”返回 null → 全灭判平局结束（draw）+ 终局幂等（ended 直接返回）+ 阶段推进入口统一 checkGameEnd 兜底（防“结算后无人可行动”挂起）
- **A2-3 好人 bot 沉默**：easy 预言家补报查验；被投票/被查杀开口辩解；平民表态/质疑（不再“p 都不放一个”）
- **A2-4 投票失真**：不确定性表达——confidenceOf 置信度低时小概率偏离最优（随机/跟风），高置信才准；缓解“太傻”与“太准”两端抱怨
- **A2-5 发言质量**：新建 server/ai/lexicon.json（意图→语料库键值，C1 未来只消费它）+ 组合式生成（prefix/core/suffix），施压/气氛/狼夜/遗言/辩护全面接入
- **A2-2 狼胜率**：配合 A2-3/A2-4 调整后跑平衡实验室 13 人局验证（见实施记录）
- **A2-6 聊天流程**：待用户明确期望，未实施（保持占位）
### ④ 基建（A3/A4/A5）
- **A3 快照迁移**：快照收纳 data/rooms.json（启动自动建目录）；根目录旧文件启动 WARN 提示手动移动（不自动迁移）；.gitignore/check-docs/check-snapshot 同步
- **A4 动态发言次数**：人类占比 >50% → 配额 1 条；>80% → 仅被质疑时开口（被投/被查杀允许额外 1 条）；否则 2 条
- **A5 confidence.js**：server/ai/confidence.js 统一置信度入口（suspicion 方差版 0.15..0.95）；B1 只换 Platt 内部实现、C1 混沌层消费同一入口；boundedCandidates 有界候选供 C1 执行层
### ⑤ 测试与部署
- 新增：check-opid.js（opId 去重 11 断言）、check-game-end.js（全灭终局 8 断言）、check-bot-expression.js（发言 4 组）、check-bot-vote-noise.js（投票波动 4 组）；更新 check-snapshot.js（data/ 路径）、check-docs.js（data/ 断言）
- 测试 41 个全绿；部署：替换 server/game/bot-brain/public/sw.js，重启，Ctrl+F5

---
## 🔧 1.6.3 —— 人机狼恋人逻辑 + 声音设置面板 + 首页精简

- **人机狼作为情侣之一（恋人互知身份，规则内）**：
  - 护恋人：人机狼恋人不刀/不魅惑恋人（人狼恋，idle/easy/smart 全覆盖）、投票不投恋人（含警长竞选/PK）、不卖狼美人带走恋人
  - 引导狼队：狼频道发言——狼队已选恋人为刀目标时劝阻并建议改刀；正常建议刀人时目标避开恋人
  - 白天辩护：恋人有嫌疑/被查杀时为恋人说话（减少全场怀疑），不施压恋人、不悍跳查杀恋人；遗言可替恋人辩护
  - 新增 `test/check-bot-lover.js`（U0-U6 专项，全绿）
- **声音设置面板**：顶栏 🔊 改为总开关，点击展开分项面板——音效总开关 + 狼嚎/天亮/投票滴答/重击死亡/翻牌/入场铃六项分开关（localStorage ww_sfx_flags）+ 上帝配音（由首页并入此处，ww_tts 沿用）；点面板外自动收起
- **起始页精简**：移除“警长选举/私聊频道/人机陪练/盗贼玩法”四个功能标签与对应样式，首页更整洁；上帝配音开关移入房间内声音面板
- 版本同步 1.6.3（页脚/sw.js CACHE/README/更新公告）；测试 36 个
## 🔧 1.6.2 —— 系统性代码审查修复

- **stepText 作用域回归（严重）**：v1.6.0 在 `renderPanel`/`applyView` 引用了 `renderNight` 函数内部的局部 `const stepText`——任何玩家第一次进入“轮到我行动”阶段（夜晚本职步骤/房主 morning/投票等）必抛 `ReferenceError: stepText is not defined`，面板冻结在旧内容、聊天/主题停更、轮询误报“连接中断”横幅（`check-client-render.js` 实测 10 处断言失败，此前 regression 未覆盖）。修复：`stepText` 提升为模块级，TTS 播报 / 后台通知 / 夜晚面板标题共用同一套文案；`check-client-render.js` 恒真断言改为验证实际覆盖的夜晚步骤数
- **人机公平化（定位：公平玩家）**：移除人机使用服务器真相作弊的路径——easy 守卫不再直接读取真实预言家身份；smart 守卫/摄梦人不再用 `campOf`（真实阵营）过滤目标；smart 女巫毒药不再避让第三方；狼队（idle/easy/smart/simulate）不再避让恋人（恋人关系对狼不可见，仅避狼队友）；保留“恋人成员互知身份”（规则内）与“第三方不投自己阵营”。同步更新 check-bot-third（U2/S1 改断言“狼不刀狼队友”）
- **警长竞选投票修复**：smart/simulate 人机此前从全体存活者中选投票目标，投到非竞选者被服务端拒绝后每轮重试直至阶段超时（警长票永远投不出）；改为只在 `candidates` 内按信念/态度评分选投（好人与狼均投“狼概率最高”的竞选者：好人阻止其当选，狼帮队友/干扰）
- **事件流补齐**：`resolveNight` 猎人开枪分支提前 return 曾丢失 `wolf_kill`/`deaths` 事件；白天放逐链（魅惑带走/殉情/枪杀）也从不推送 `deaths`——现按批次补齐，`/api/debug` 勘查数据完整
- **死代码清理**：decisionSmart 重复 `case 'dreamer'`（第二分支永不可达）、`ATT_STATE`/`isThirdMember` 未使用、game.js `DEATH_TEXT` 未使用、`pickedRole` 字段与 `effRole` thief 分支（从未赋值）、`cupidCamp` 无用参数、`getWolfCount` 的 settings.counts 回退（字段从未存在）、SSE 客户端 `j.gone` 死分支、reveal 路径重复 `bump` 与缩进错乱
- **错位整理**：server.js 环境常量统一上移到文件顶部（原声明在使用之后）；TTL 清扫补清 `_botTimer`（与 restoreRoom/onBroken 两处一致）；rateLimit 注释修正（仅回环不限流，局域网/公网照常——公网部署下 socket 对端可能是反代内网地址，私有段豁免会导致限流失效）；index.html 页脚版本 v1.3.0 → 1.6.2；sw.js CACHE 升至 ww-v1.6.2；README 测试计数修正
- **健壮性**：`/api/action`、`/api/chat`、`/api/advance`、`/api/leave`、`/api/kick` 增加 room/me 参数格式校验（与 `/api/state` 同规则）；猎人开枪面板文案区分“被狼人杀害/被投票放逐”
- **版本串同步检查**：新增 `test/check-version-sync.js`，校验 package.json / index.html 页脚 / sw.js CACHE / README / 更新公告.md 五处版本一致；测试 35 个
## 🔒 1.6.1 —— 代码审查修复 + 不变式自检回滚

- **狼人投票方向**（simulate 档）：此前非 charge 狼投“分数最高”= 最像狼的人 = 投狼队友/错投好人；修正为狼先取反（投最不像狼的），shark 风格再对狼队友减分
- **狼总数先验**：`getWolfCount` 改取 `room.roleCounts.wolf`（配置值，不随死亡漂移）；修复前回退逻辑统计活狼导致 calibrateBeliefs 把狼总数越校准越小
- **第三方阵营判定**：投票/卖狼美人逻辑由 `campOf`（只分狼/好）改 `factionOf`（含人狼恋第三方），人狼恋狼恋人不再被误判为狼队
- **引擎不变式自检（B1）**：`bump()` 时 O(房间) 轻量校验（players 结构/夜晚步骤合法性/reveal/lastVoteResult），失败 → `setOnBroken` 回调 → server 从最近快照回滚该房间（局级事务，不污染全局）；连续 3 次触发则解散房间防回滚风暴；系统消息“⚠️ 房间检测到异常，已自动回滚到最近存档”
- **onChange 值导出 bug**：CommonJS `module.exports = { onChange }` 导出的是 null 值，`Game.onChange = fn` 赋值实际不生效（快照防抖只靠 API 层 markDirty）；改 `setOnChange/setOnBroken` setter 导出，并在 loadSnapshot 之前注册
- **限流防伪造**：仅当请求来自本机代理（cloudflared/Render）时才信任 CF-Connecting-IP/x-forwarded-for；公网直连一律用 socket 地址
- 细节：healthz 503 统一头、readBody 413 主动 resume、快照序列化补 Map 防御、README 测试清单补齐 32 个
- 新增 check-invariants.js（自检不误报/破坏触发/快照链正常）
## 🚀 1.6.0 —— 通知 / 配音 / 事件流 / 平衡实验室

- **后台通知**：挂机切后台不再错过回合（进房请求权限，轮到我时页面隐藏才弹）
- **上帝配音**：`speechSynthesis` 零依赖，夜晚开始播“天黑请闭眼”、每步骤播“XX请睁眼”；首页「🔊上帝配音」开关（默认关，localStorage ww_tts）
- **游戏事件流**：每房间 `events` 环形缓冲 200 条（night_start/night_step/wolf_kill/deaths/exile/shot），`GET /api/debug?room=` 拉取——运维现场勘查 + 上帝视角回放的数据基础
- **蒙特卡洛平衡实验室**：`node test/check-balance-lab.js --games=100 --cap=10` 等参数批量自动对局（smart/simulate 人机），输出阵营胜率/首刀分布/平均局时——规则平衡从此有数据答案（当前 8 人局 smart 样本：狼约 60~80%、好 20~40%，人机行为与真人差异会放大狼胜率，作相对比较用）
- 新增 check-debug.js（事件流 6 断言）；测试 31 个
## 🔧 1.5.7 —— 快照恢复修复

在对v1.5.6 进行评审时所发现的两个必改项：

- **P1**：`resumeRoom` 漏了 `hunter_shot` 分支——快照发生在“白天被放逐的猎人等待开枪”时，恢复后弃枪定时器不会重挂，白天流程永久卡住（只能房主强推救场）。已补 `hunter_shot → scheduleHunterShotTimer(room, remain(...))`，`check-resume.js` S1/S2 断言覆盖
- **P2**：`.gitignore` 补 `rooms.json.bak`（快照含玩家 ID、昵称、完整身份牌，严禁入库）
- 顺带修复 `resumeRoom` 两个定时器（进夜/盗贼）闭包漏传 `room` 的隐患
- 新增 `check-resume.js`：恢复定时器重挂专项（hunter_shot/夜晚猎人/进夜/盗贼/白天阶段，5 断言）
## 🛠️ 1.5.6 —— 工程加固

- **跨局记忆治理**：rematch/startGame 统一 `resetBotPerGame`（重置 beliefs/seerClaims/roleClaims/silverWater/guarded/attitudes/各去重集合，保留 suspicion），修复三个跨局副作用（悍跳标记/魅惑目标/银水泄漏）；rematch 时把上一局狼名单显式注入新局 suspicion（+15，“真人记得上局谁是狼”的显式建模）
- **房间快照**：`rooms.json`（已 gitignore，含隐私数据）——变更防抖 1s 原子写 + 每 60s 兜底 + SIGTERM/SIGINT 优雅保存；启动自动恢复房间并对局进度（定时器按状态重挂、bot 调度唤醒、Set 记忆 replacer/reviver 安全序列化）；格式带 version，不兼容则安全丢弃绝不崩启动；`SNAPSHOT_SEC=0` 禁用（测试环境）
- **内存看门狗**：RSS 超过 `MAX_RSS_MB`（默认 400）30s 检测主动退出让平台/脚本重启；`/healthz` 返回 rss/rooms，超限回 503
- **防滥用**：IP 令牌桶限流（create 10/分、join 30/分；`CF-Connecting-IP` 取公网真实用户，本地回环不限流不误伤测试）+ 全局房间上限 300
- 新增 `check-snapshot.js`（快照生成/杀进程恢复/继续对局/Set 恢复）；测试 30 个
## 🌐 1.5.5 —— 快速隧道代码侧加固

Render 绑卡受阻、走快速隧道的前提下，从代码侧改善隧道断连体验：

- **PWA 非 2xx 回退**：隧道断连（524/502/504）时 Service Worker 返回缓存页面而非错误页——页面能打开，界面显示重连中，隧道恢复自动恢复（`sw.js`，缓存版本升至 ww-v1.5.5）
- **房间 TTL 分阶段**：lobby/已结束房间 30 分钟回收（挂机房及时清理），已开局对局 120 分钟（断线 2 小时内都能恢复，`ROOM_LOBBY_TTL_MIN`/`ROOM_TTL_MIN` 可调）
- **keep-alive 65s**：node 默认 5s，调大后减少与 cloudflared 的连接重建
- 全量 29/29 测试通过（check-security 的 TTL 专项已适配新常量）
## 🌐 1.5.4 —— 公网稳定性加固

**现象**：公网对局中出现大量 `Incoming request ended abruptly: context canceled` + 界面 524。

**诊断**：本地压测（决策层 2.3 万次高频调用 <1s、服务器 2 日夜循环 + 5 条 SSE + 持续健康监控最大延迟 3ms）确认代码库健康。524 来自 **Cloudflare 快速隧道（trycloudflare）本身**——官方明确声明无 uptime 保证；SSE 长连接在 http2 快速隧道下会被边缘取消，客户端重连又新建长连接 → 连接取消风暴 → 界面超时。

**加固**：客户端 SSE 连续失败 3 次自动降级为**纯轮询**（不再尝试长连接），页面靠轮询存活；进房时重置降级状态。

## 🔧 1.5.3 —— 前端修复

大厅「新加人机级别」的「模拟」按钮点击不高亮：`setBotLevel` 的级别白名单在 v1.5.0 加 simulate 档时漏了校验，点击直接 return。已补全（idle/easy/smart/simulate），并统一按钮 class 格式；渲染层验证 4/4（初始不高亮/点击高亮/切回取消/非法值拒绝）。
## 🎯 1.5.2 —— 策略优化批（投票集中 / 卖狼美人 / 保命与选牌）

- **投票集中**：白天投票优先投「嫌疑排名前二且已有人投」的目标，防多 bot 分票导致平票（smart/easy/simulate 三档统一）
- **卖狼美人**：狼美人魅惑高价值目标（可信预言家/自称神职）且狼数充足（≥2）时，狼队主动投狼美人——放逐带走魅惑目标（1 狼换 1 神职）；魅惑目标经狼队 `wolfPackMemory` 共享
- **守卫守神职**：优先守可信预言家/自称神职者（屠边保护神职），其次守狼概率最低者
- **摄梦人保命**：摄梦人自认将在下一夜被刀（自己可信预言家/自称过神职）→ 梦狼概率最高者（带走一个狼）；否则保护狼概率最低者
- **盗贼选牌**：无狼时偏向选神职卡（预言家/女巫/守卫/摄梦人/猎人 > 平民）
- **职业专属发言**：预言家 3 句式报查验、女巫报银水 3 变体、守卫 3 变体、猎人亮枪、摄梦人亮梦、狼美人魅惑威胁自曝（配合卖狼美人）、丘比特亮身份、被查杀穿衣服补猎人/摄梦人

新增 `check-bot-opt.js`（O1-O6：跟票/卖狼/守神职/摄梦人保命/盗贼选牌/职业发言），测试 29 个脚本。
## 🧩 1.5.1 —— 第三方阵营适配（人狼恋 / 丘比特认知）

补齐 bot 对**第三方获胜逻辑**的认知——引擎的 `checkWin` 本就支持第三方独赢（人狼恋情侣/丘比特第三方：场上仅剩第三方成员即胜），但 bot 之前的阵营判断只有「狼/好」两分，第三方 bot 会把自己当好人打、狼 bot 可能刀死恋人触发殉情连锁减员：

- **factionOf(room, p)**：对齐引擎 cupidCamp/thirdFaction（人狼恋→第三方、全狼/全好情侣随阵营、丘比特自连→第三方），并导出供测试
- **狼**：夜晚刀人/魅惑/投票全部排除第三方（第三方是狼的「隐性盟友」——让第三方耗好人）；idle 档补狼刀同样排除
- **第三方 bot**：投票不投自己阵营（保恋人保自己，恋人死=自己殉情）；女巫恋人被刀必救、毒药避开恋人
- **神职声称优先级**：狼刀目标 = 可信预言家 > **自称守卫/女巫/猎人者** > 狼概率最低者（发言提取，独立去重）

新增 `check-bot-third.js`（U1 factionOf 7 断言 / U2 狼不刀恋人 / U3 第三方不投恋人 / U4 神职声称刀 / S1 人狼恋黑盒两夜），测试 28 个脚本。
## 🎲 1.5.0 —— simulate 态度模型档（马尔可夫 + 多证据源 + 双校准）

新增第四档人机 `simulate`：在 `smart` 贝叶斯之上叠加**5 状态马尔可夫态度模型**（怀疑极端→信任极端），每个玩家一条态度分布，随回合自然演化并按证据更新：

- **证据源**：查杀/金水发言（CHAT_BAD/GOOD）、放逐投票（VOTE_AGAINST）、被狼刀死亡（DEATH）、被女巫毒死（POISON）、警长票（SHERIFF）——均已适配项目真实字段（补丁假设的 `sheriffVotes`/`lastWitchPoison` 不存在，改为从 `lastVoteResult.kind` 与死亡事实提取）
- **风格矩阵**：aggressive（易极端）/ balanced / conservative（慢热）；后期夜晚矩阵对角线增强（更集中）
- **双校准**：贝叶斯 wolfProb 与态度预测各经 Sigmoid 压缩后按激进度加权，避免分数过度极端
- **狼队风格**：`charge`（反向评分：专投好分数最低的）/ `shark`（避开狼队友）
- **发言**：白天先走 smart 话术，无话题时按态度评分点名“值得关注”；发言证据**已去重**（补丁缺陷：同一条消息会被多次决策反复应用）

大厅「新加人机级别」新增**模拟**档；`add_bot` 支持 `style`/`wolfStyle` 参数。新增 `check-bot-simulate.js`（U1-U4 单元 + S1-S3 黑盒），测试 27 个脚本。
## 🗣️ 1.4.4 —— 发言体系升级（辩论/穿衣服/气氛）

此前 bot 白天只有一句“报查验/悍跳/报银水”固定话术。本次升级为完整的发言体系：

### 白天每人每天至多 2 条（主发言 + 回应/辩论）

- **主发言**：预言家报真实查验、狼悍跳预言家（每队一次）、女巫报银水、守卫报守人（模糊）、施压表态；无话题时随机气氛闲聊
- **次发言（回应/辩论）**：
  - **被查杀 → 穿衣服自证**：守卫“我是守卫，你查杀我就是在自爆”、女巫“解药还在，投我等于帮狼赢”、预言家“我才是真预言家”、平民“投我不亏但浪费轮次”、**狼被查杀反穿守卫衣服**“你查杀我说明你就是狼”
  - **对跳辩论**：预言家“X 在悍跳预言家，我才是真的，查验记录都在 / X 查杀的人我验过是金水，他在乱带节奏 / 今晚我验 X”；狼“X 才是狼，狼队急了乱咬”
  - 施压跟票、气氛插科打诨

### 新增发言场景

- **狼人夜晚狼频道**：狼 bot 出刀前在狼频道沟通（“先刀预言家，稳赚不亏 / 刀 X 吧，他太跳了 / 别刀队友啊喂”），每狼每晚一条，真人狼可见
- **遗言**：smart 角色有信息量遗言（预言家报昨夜查验、守卫/女巫/猎人跳身份、狼隐藏身份“我是平民”），easy 简短，idle 沉默

### 修复

- **狼频道发言时机**：wolf_set 成功后 setNightStep 会把 phase 推进到早晨（狼频道权限随 phase 关闭）→ 发言移到**出刀前**（语义也更合理：先商定后出刀）
- 测试稳定性：bot 完整流程测试改为“成功即止，失败重试”模式（容忍偶发调度竞态）；check-allroles 盗贼选牌改试错法（thiefCards 为中文名）

### 涉及文件

`bot-brain.js`（话术库 + 辩论/穿衣服/狼夜/遗言）、`game.js`（发言计数 2 条 + 狼夜发言钩子）、新增 `test/check-bot-debate.js`（B1 对跳辩论 / B2 狼夜频道 / B3 遗言）

- 回归：26/26 全过（含稳定性 5/5 验证）
- 升级方式：替换 `game.js` + `bot-brain.js`，**重启服务器**（发言为服务端行为），浏览器 Ctrl+F5

---
## 🧠 1.4.3 —— 人机高阶推理 + 发言模拟

对应此前提出的四项 bot 局限优化：

1. **女巫银水/守卫守人信息利用**：女巫 bot 记住救过的人（银水），持续视为好人证据（LR 0.05，强于他人查杀——被查杀也能靠银水自证）；守卫 bot 记住守人目标（LR 0.7 弱证据）
2. **预言家查验历史**：smart 预言家**优先查验对跳者**（白天声称过预言家的人），其次才查狼概率最高者
3. **狼美人/情侣**：魅惑策略升级——优先魅惑**高可信预言家**（狼视角真相校准过），其次最可信好人；不魅惑狼/不魅惑刀目标
4. **发言模拟**：白天每 bot 每天至多一条（真人同款 chat 通道）：smart 预言家报查验、女巫报银水、**狼队悍跳预言家**（每队一次）、好人施压；easy 仅"我怀疑 X"；idle 沉默。附带**对跳存疑**：好人视角出现多个预言家声称时全存疑，不被悍跳误导

### 修复（黑盒测试逼出）

- **botDelay 负延时 bug**：1.4.1 的 `±2500ms` 是绝对抖动，测试设 `BOT_DELAY_MS=400` 时会算出**负数**（setTimeout 立即执行 → bot 行动风暴/调度竞态）。改为**相对 ±25%**（10s→7.5~12.5s，测试 400ms→300~500ms）
- **`nameById` 缺失**：发言功能引用了不存在的工具函数 → botTalk 抛异常 → runBots 中断
- **`markBotTalked` 与夜晚 null 决策**：发言调度标记与夜晚决策互不干扰（night 的 null 决策不再污染白天发言去重）

### 涉及文件

`bot-brain.js`（四项推理 + 发言）、`game.js`（discuss 发言调度 + markBotTalked + botDelay 修复）、新增 `test/check-bot-advanced.js`（A1 银水不投 / A2 对跳查验 / A3 魅惑策略 / A4 报查验 / A5 悍跳 / A6 挂机沉默）

- 回归：25/25 全过
- 升级方式：替换 `game.js` + `bot-brain.js`，重启服务器，浏览器 Ctrl+F5（新增 bot 发言为服务端行为，必须重启）

---
## 🩹 1.4.2 —— 修复：夜晚行动按钮与频道消失

**症状**（真实对局反馈）：进入夜晚后无行动按钮、狼人/情侣频道从未出现，45s 倒计时后被跳过直接进入白天。

**根因**：`public/client.js` 的 `renderPanel()` 夜晚分支存在**变量名笔误**（v1.3.0 引入）——定义了 `const STEP_GLOW = {...}[nstep]`，下一行却引用了不存在的变量 `glow`（`if (glow) ...`）。进入夜晚时 `renderPanel` 必抛 `ReferenceError: glow is not defined` → **整个面板渲染中断**（行动按钮不渲染 + 频道 tabs 也不渲染）→ 服务端照常 45s 超时跳过。

**为什么之前没被发现**：全部自动化测试只覆盖引擎层与首页，**客户端夜晚渲染从未被自动化执行过**。本次新增：

- **修复**：`if (glow)` → `const glow = {...}[nstep]`，一处笔误
- **新增 `check-client-render.js`**：DOM stub harness（`_harness-dom.js`）在 Node 中直接执行 `client.js` 的渲染链，用引擎驱动的真实 view 逐阶段渲染——夜晚每个职业步骤（各角色视角）、狼人/情侣频道 tabs、白天各阶段（night/morning/lastword/sheriff_campaign/discuss/vote）全部断言无异常。**今后任何渲染层回归都会被拦住**

### 涉及文件

`public/client.js`（笔误修复）、新增 `test/_harness-dom.js` + `test/check-client-render.js`、README（测试清单 24 个脚本）

- 回归：24/24 全过
- 升级方式：仅替换 `public/client.js`（其他文件无改动）→ 浏览器 **Ctrl+F5 强刷**即可，无需重启服务器

---
## 🏝️ 1.4.1 —— 节奏调整 + 离线模式 + 全职业回归验证

**背景**：真实多人局反馈“所有职业无法使用能力、狼人/情侣频道消失”。经排查与回归验证（新增 `check-allroles.js`，走真实 HTTP 路径模拟客户端：盗贼局 10 人全职业）：**当前代码库身份发放、各职业夜晚行动、狼人/情侣频道、狼美人出刀均正常**。此类症状在部署环节的常见成因：

- **浏览器缓存旧 `client.js`**（新 server + 旧前端或反之，UI/逻辑不匹配）→ 务必 **Ctrl+F5 强刷**
- **服务器未重启**（node 进程常驻旧代码；SSE 端点 1.1.0 起就要求重启才生效）
- **文件替换不完整**（新增的 `bot-brain.js` 缺失会导致 `game.js` require 失败、服务无法启动）
- 盗贼局中盗贼若长时间未选牌，30s 超时自动代选（有狼必选狼），房主也可随时强推——身份不会永久卡住

### 调整与新增

- **倒计时**：夜晚每职业环节 30s→**45s**；白天发言/投票 30s→**60s**（均可被 `PHASE_TIMEOUT`/`NIGHT_TIMEOUT` 覆盖）
- **人机行动节奏**：所有 bot 在可行动环节先等待 **10s±2.5s** 再行动（模拟真人思考；`BOT_DELAY_MS` 可覆盖，测试用 400~600ms 加速）
- **狼美人出刀**：确认引擎本就支持（`wolf_set` 对狼美人开放，`nightActors('wolf')` 含狼美人），`check-allroles.js` 断言覆盖
- **离线模式**：首页新增「🎮 离线模式（单机·智能人机）」——一键建房 + 自动加满 5 名智能人机（6 人局），进房后点「开始游戏」即玩
- **smart 摄梦人**：从公共层随机升级为「梦狼概率最低的非狼玩家」（保护最可信好人）；easy 保持随机

### 涉及文件

新增 `test/check-allroles.js`；修改 `game.js`（倒计时/botDelay）、`bot-brain.js`（摄梦人策略）、`public/index.html`、`public/client.js`（离线模式）、`public/style.css`、`test/simulate-bot.js`、`test/check-bot-smart.js`（BOT_DELAY_MS 加速）、README/公告（测试清单 23 个脚本）

- 回归：23/23 全过（新增 check-allroles：身份发放 10/10、盗贼选牌、夜晚 cupid→lovers→guard→dreamer→wolf→seer→witch 各职业 action、狼美人出刀）
- 升级方式：替换 `game.js` + `bot-brain.js` + `public/`，**重启服务器**，浏览器 **Ctrl+F5**

---
## 🧠 1.4.0 —— 人机三档决策

**定位**：把原来「简单AI / 挂机」两档人机升级为**每台人机独立可选**的三档（挂机 / 简单 / 智能），其中「智能」带贝叶斯推理与狼队协同，人机不再只是陪跑，能成为有威胁的对手。

### 级别定义

| 级别 | 行为 | 对应旧设置 |
|---|---|---|
| **挂机 idle** | 仅补必要动作（被刀自救/全员人机时补狼刀），白天一律弃票，不参与分析 | passive |
| **简单 easy** | 关键词嫌疑度：解析「查杀/金水/是狼/是好人」，被查杀者嫌疑 +50，可投被查杀者；狼视角会优先刀声称预言家的人 | auto |
| **智能 smart** | 贝叶斯推理：以查验/查杀/金水/狼刀死亡/放逐投票为证据更新狼概率；预言家对跳推理；狼人视角用真相校准声称可信度、优先刀高可信预言家；多狼 bot 信念取平均共享；狼数约束校准 | 新增 |

### 关键实现

- **`bot-brain.js` 独立模块**：`createBotDecision(room, p)` 统一入口，公共层（盗贼选牌/遗言/警徽/竞选/丘比特/情侣/摄梦）三档一致，智力决策点（狼刀/查验/守卫/女巫/投票）按级别分发
- **字段映射**：开源补丁的 `seer_set/guard_set/witch_set`、`seerChecked/night.killed/witch.save/lastExecutedId` 均不存在于本项目，已全部映射到实际结构（`seer_pick/guard_pick/witch_act`、`room.night.wolf.kill`、`witchPots`、`seerHistory`、`lastVoteResult.exiled`）
- **公平性修正**：预言家声称的可信度校准**仅狼 bot 可用**（狼知道谁是真狼，合法）；好人 bot 只做「对跳」推理（同目标反结论 → 降可信），不用真相作弊
- **同分随机**：无证据时所有玩家概率相等，所有「选最值」处先随机打乱，避免固定偏向某座位（曾导致 smart 狼夜1必刀 1 号位）
- **兼容**：`botLevel` 未设置时按房间 `botMode` 映射（passive→idle、auto→easy），旧存档/旧玩法零影响；`add_bot` 支持 `level` 参数，非法值忽略
- **前端**：大厅「人机调试」区新增「新加人机级别」三选按钮（挂机/简单/智能），添加时固化到该 bot

### 涉及文件

新增 `bot-brain.js`、`test/check-bot-smart.js`；修改 `game.js`（require + botDecision 转发 + add_bot level）、`public/client.js`、`public/style.css`、README/公告（测试清单 22 个脚本）

- `check-bot-smart.js`：5 项断言（level 参数校验 / smart 狼夜2刀跳预言家者（黑盒端到端）/ smart+easy 投被查杀者（决策层单元））
- 回归：simulate-bot（auto/passive 行为保持）等 22/22 全过
- 升级方式：新增 `bot-brain.js` + 替换 `game.js` + `public/`，重启服务器，浏览器 **Ctrl+F5**

---
## 🎮 1.3.0 —— 对局体验 + 社交传播

**定位**：让「我的行动有回应」（反馈动画/震动）、「拉人更快」（邀请链接）、「复盘有话题」（赛后趣闻），并把应用常驻桌面（PWA）。

### 对局体验

- **② 行动反馈动画**：验/守/梦/刀/魅/救/毒/枪成功后，目标卡闪现职业图标（🔮金光/🛡️盾牌/😴Zzz/🔪刀光/💘红心/💊解药/🧪毒药/🔫枪口），复用 `spawnFx` 动效层，动画完自毁、减少动效时跳过
- **③ 房主“谁已投/已行动”明细**：`view.vote.votedBy` / `view.sheriffVote.votedBy` 仅房主下发（含投给谁与弃票 null），非房主一律 `undefined`；面板显示「👁️ 房主·已投明细」
- **④ 移动端震动**：轮到我行动短震 60ms、自己死亡长震 300ms、被踢两段震；减少动效时静默

### 社交传播

- **⑤ 邀请链接**：`?room=XXXXXX` 直达（首页自动填入+高亮，不误入）；顶栏「复制」/📤 原生分享面板/创建大卡均复制完整邀请链接
- **⑥ 赛后趣味统计**：整局本地累积（每晚死亡名单 + 全体频道发言数，消息按 id 去重），结算页展示「最话痨/最快出局/最惨烈之夜」；rematch 自动清空
- **⑦ PWA 可安装**：`manifest.json` + `icon.svg` + `sw.js`（网络优先、失败回退缓存、`/api/` 永不缓存、发版换 CACHE 版本号自动清旧）

### 视觉与无障碍

- **⑧ 环境粒子**：夜晚流星、白天阳光粒子，28s 低频随机触发（减少动效时静默）
- **⑨ 角色睁眼氛围**：夜晚面板光晕随步骤变角色色（预言家蓝/女巫紫/守卫绿/狼红…）
- **⑩ 字号调节**：首页 A-/A/A+ 三档整页缩放（zoom），localStorage 记住

### 涉及文件

`game.js`（votedBy 仅房主）、`public/`（index.html/style.css/client.js/manifest.json/icon.svg/sw.js）、`test/check-votedby.js`、`test/check-pwa.js`（新增，测试清单 21 个脚本）

- `check-votedby.js`：10 项断言（房主明细/非房主不泄露/弃票 null/实时更新/警长投票同规则）
- `check-pwa.js`：8 项断言（manifest/sw/icon 200 + MIME + 字段 + API 不缓存）
- 升级方式：替换 `game.js` + `public/`，浏览器 **Ctrl+F5** 强刷；PWA 首次访问后自动注册 Service Worker

---
## 🏠 1.2.0 —— 首页焕新 + 在线统计

**定位**：把「进入房间」从两步折叠交互改成一眼看懂的双卡入口，并给首屏补齐氛围与「有人在线」的社群感。

### 核心体验（T1）

- **① 双卡入口**：创建 / 加入改为两张功能卡（加入卡输入框**常显**，不再折叠），整卡可点、键盘可达（Enter/Space）；创建后无需再点一次即可在另一张卡输房号
- **② 趣味昵称生成器**：无昵称首访自动生成「形容词 + 动物」昵称（神秘小狐狸 / 熬夜猫头鹰…），与座位动物头像风格统一，告别「玩家123」；🎲 按钮一键换一个
- **③ 房间号实时校验**：输入即转大写；6 位合法 → 金色高亮并自动进入；含非法字符 / 房间不存在 → 红框抖动 + 错误提示
- **④ 创建成功「房间号大卡」**：全屏庆祝遮罩 1.5 秒——房号逐字弹出 + 复制按钮 + 进度条，随后自动进入房间（尊重减少动效开关）

### 氛围与信息（T2）

- **⑤ 首屏夜景升级**：右上角圆月光晕呼吸、2~3 片云层缓慢横移、狼影水印改为底部呼吸剪影（prefers-reduced-motion 时全部静止）
- **⑥ 玻璃卡片**：首页卡片与顶栏 blur 语言统一（backdrop-filter 磨砂 + 顶部金色光边 + 入场动画）；双卡 hover 微抬升 + 金色光边，与 role-card 交互语言一致
- **⑦ 功能标签 chips**：警长选举 / 私聊频道 / 人机陪练 / 盗贼玩法 四个卖点标签
- **⑧ 页脚**：版本号 + 「零依赖 · 公网可玩」

### 进阶（T3）

- **⑨ 在线房间统计**：新增 `GET /api/stats` → `{ rooms, players }`，首页显示「🔥 当前 X 个房间正在开黑」（每 30 秒刷新）；与 TTL 共用 `lastActive`，超过 30 秒无轮询/SSE/操作视为非活动房间不计入（阈值可用 `STATS_ACTIVE_SEC` 调整，默认 30）；房间解散自动减数
- **⑩ 入场音效**：首次交互两声轻铃（Web Audio，沿用音效体系；**主题切换未做**——游戏内已有昼夜主题，首页再叠主题开关与 applyTheme 冲突且收益低，挂起）

### 涉及文件

`server.js`（/api/stats + STATS_ACTIVE_SEC）、`public/index.html`、`public/style.css`、`public/client.js`、`test/check-stats.js`（新增）、`test/client-flow.js`（首页断言更新）

- 新增 `check-stats.js` 专项：初始 0 / 创建计入 / 加入 +1 / 空闲过滤 / 轮询恢复活跃 / POST 404 / 解散归零，全部通过
- 升级方式：替换 `server.js`、`public/` 即可；浏览器 **Ctrl+F5** 强刷

---
## 📡 1.1.0 —— SSE 推送唤醒

**架构**：新增 `/api/stream`（SSE）端点，服务端每秒扫描订阅房间的版本号，变化即推送 `{v}`（几字节）；客户端收到推送立即拉 `/api/state`。**数据源不变**（状态永远走轮询 API），SSE 只负责「唤醒」。

- **推送即时性**：操作（投票/发言/夜晚推进等）后对方标签页几乎立即刷新，不再等轮询间隔
- **带宽大降**：SSE 正常时轮询从 700/1600ms 降为 **30 秒 1 次心跳**；漏推最多 30s 自愈
- **断线自愈**：SSE 断开 → 立即回退常规轮询（含指数退避 2s→4s→8s→15s）；5 秒后自动重连；任一路径成功即恢复
- **参数校验**：房间号/玩家 ID 格式校验 + 玩家存在性校验，非法连接 404
- **TTL 兼容**：SSE 订阅在服务端计入 `lastActive`，不会误回收活跃房间；房间解散/回收时服务端主动结束所有订阅连接
- **隧道监控**：`开启公网联机.bat` 为 cloudflared 增加 `--metrics localhost:39571`，可查 `http://localhost:39571/metrics`
- **心跳保活**：25 秒 SSE 注释行 ping，防中间设备掐空闲连接
- 新增 `check-sse.js` 专项：合法性 404 / 初始推送 / 版本变化推送 / 房间解散关连接，全部通过
- 升级方式：替换 `server.js`、`public/client.js`、`开启公网联机.bat` 即可；客户端强刷

---
## 🧪 1.0.2 —— 测试覆盖补全

- 新增 `check-gaps.js`：7 个此前未覆盖的引擎规则专项全部落地并通过（**引擎零改动**，行为与 `rules.md` 完全一致）：
  - G1 女巫被狼刀时自救 → 平安夜（场景3 只测过救别人）
  - G2 摄梦人夜间出局 → 梦游者一并出局（deadBy=dream）；注：测试需屠城模式（4 人局唯一神职死亡会屠边提前结束）
  - G3 猎人被毒杀不能开枪（无 hunter_shot 阶段）
  - G4 守卫连守同一人被拒绝（改守他人后流程正常不卡）
  - G5 警长竞选全员弃权 → 无人当选（sheriff=null，结果 none）
  - G6 PK 再平票 → 无人出局，直接入夜（场景5 只测过 PK 一轮出结果）
  - G7 房主离开 → 房主转移给最早加入的真人（跳过人机）；无真人 → 解散

---
## 🔧 1.0.1 —— 规则补丁与测试加固

**说明**：1.0.0 发布后的第一轮测试审查补丁。

- **引擎规则**（rules.md 三.10）：已出局玩家在活跃投票阶段（放逐投票/警长投票/PK 投票）提交投票一律拒绝 `你已出局，无法投票`——原先仅计票时跳过，脏票仍会写入 `room.votes`（规则卫生问题）
- **测试假通过修复**：
  - simulate 场景1「死者投票被拒」断言原先在游戏结束阶段执行（phase=ended 时任何动作都返回错误，断言恒过）→ 挪到活跃放逐投票阶段真实验证
  - simulate 场景2「狼美人被放逐带走魅惑者」断言原先守卫 `!allDeaths[v1]` 恒为假（魅惑死于白天，dayDeaths 已并入 allDeaths）→ 断言从未执行，改为直接查 dayDeaths——这条核心规则第一次被真正断言
  - **测试驱动适配**：check-handover / check-hunter-shot / check-hunter-timeout 三个驱动的投票循环此前依赖「死者投票被静默忽略」才能跑通，现改为只代存活玩家操作（引擎修复的连带修正，也反证了原漏洞真实存在）
- **测试基建**：check-handover 端口 8367 → 8363（与 simulate-cupid-self 撞端口，并行跑会抢）
- **文档**：警徽移交范围（非魅惑/摄梦/毒杀均可移交）为 rules.md 关键规则裁定 #2 正式规则，check-handover.js 头部注释显式挂钩该条款并注明「改规则需同步改测试」

---
## 🚀 1.0.0 —— 正式发布

**发布说明**：自 **rc.4 起进入真实对局内测**，历经 3 轮真实对局验证（rc.4/rc.5/rc.6），对局中暴露的规则问题已全部定稿（见 `rules.md`）。本版把 rc.6 之后积累的全部迭代（交互/视觉/音效/安全/修复）合并为正式发布。

### 交互与视觉（UI/UX v3 + Visual v1–v4）

- **UI/UX v3（36 项）**：桌面双栏布局、移动端聊天抽屉、顶栏（阶段面包屑/倒计时脉冲/强制继续智能点亮/身份大卡弹窗/房号点击复制/离开二次确认）、首页（记住昵称/上次房间一键重进/规则速览/6 位房间号自动进入）、聊天（气泡化+自己右对齐/智能滚动/私聊未读红点/快捷短语/遗言卷轴样式）、遮罩（睁眼渐入/角色大卡/天亮死亡摘要）、toast 类型化四色、无障碍（reduced-motion/aria-live/focus-visible）
- **Visual v1–v4**：暗夜风+玻璃顶栏+阵营配色、昼夜主题切换+星空眨眼、身份卡翻开动画、死亡红闪、行动面板呼吸光圈、职业专属光晕、**天亮死亡翻牌**（规则自带环节：翻面露身份+死因，平安夜金色光芒）、投票结果揭晓（数字滚动+放逐碎裂+平票对撞）、警徽移交飞行动画、选人扫光+动作图标、狼人行动面板泛红、胜利横幅光效
- **音效**：Web Audio 零依赖合成（狼嚎/天亮和弦/投票滴答/死亡重击/翻牌沙沙）+ 顶栏静音开关
- **通用交互**：操作防连点、弱网提示横幅、快捷键（1~9 选人、0=10 号位、Enter 确认）、减少动效开关（无障碍/低性能设备）、全局动效层 `spawnFx`（动画完自毁，不参与轮询重绘）
- **移动端**：玩家列表横向滚动卡片、行动区吸底、安全区适配

### 规则与玩法（自 rc.6 后调整，均已写入 `rules.md`）

- 白天被放逐的猎人 30 秒超时弃枪；夜晚每步与盗贼选牌加 30 秒倒计时（未操作自动跳过）；夜晚开始 30 秒自动跳过（不再卡死）
- 警徽移交规则：非「魅惑/摄梦人/毒杀」死因均可移交（放逐/枪杀/狼刀/殉情可）
- 盗贼选牌后 5 秒展示窃走结果，确认后才开局；两张牌含狼时红框+「必须选狼」警示
- 聊天限流 800ms/条；离开/已出局玩家禁止发言与动作（防带宽 DoS）
- 房间 TTL 回收（2 小时，可配置）

### 稳定性与安全

- 全面安全+死代码审查：MOODS 表情白名单下沉服务端、同归于尽判定顺序成文、清理遗留死代码
- 玩家反馈修复：盗贼自选牌可见性回归、聊天重复发送（消息去重+轮询串行化+输入法守卫）、放逐投票「点不动」（重绘守卫+关键按钮 onpointerdown 三层修复）、「添加人机」点击失效（onclick 按钮改 click 阶段锁定，避免 pointerdown 禁用吞掉 click）
- 代码评审修复：身份大卡技能描述恒空、init 空值守卫、快捷短语注入、`#info`/`#panel` 双 sticky 重叠、牌背字形跨平台、聊天气泡动画收敛、死亡播报独立配色、死代码清理等

### 测试

16 个自动化脚本全部通过：`simulate.js`（主流程+场景 1~9+丘比特自连/第三方/倒计时/狼美人枪杀）、`client-flow.js`（浏览器流程）、`check-security.js`（路径穿越/参数校验/413/TTL）、`check-opt.js`（优化）、`check-handover.js`（警徽移交）、`check-night-timeout.js`、`check-thief-view.js`、`check-chat-limit.js`、`check-hunter-timeout.js`、`simulate-bot.js`（人机）

### 升级方式

替换 `server.js`、`game.js`、`public/`、两个 bat、`package.json` 即可，无需安装依赖；浏览器 **Ctrl+F5** 强刷。

---
## 🐛 1.0.0-rc.6 —— 关键 Bug 修复与文档对齐

**此前的问题**：狼人没做任何选择直接点"确认行动"会误发空刀（覆盖全队刀人选择）；聊天切换频道时消息可能不刷新；放逐/当选警长结果的玩家名未转义；CSS 中 `#overlay-night` 定义了但从未使用；部分文档与实际代码不一致。

### 修复内容

- **🔴 狼人"确认行动"误发空刀（关键 Bug）**：阶段切换重置草稿时 `kill/charm` 初始值为 `null`，而确认逻辑以 `!== undefined` 判断是否携带 → 未做任何选择直接确认会误发 `kill:null`（空刀）+ `charm:null`（不魅惑），覆盖全队刀人选择。已改为初始值 `undefined`，5 个用例验证通过（未操作确认 / 显式空刀 / 正常选刀 / 显式不魅惑 / 显示回退）。
- **聊天切换频道不刷新**：`lastChatCount` 为全局单值，两频道消息数恰好相同时切 tab 不重绘。新增 `lastChatTab` 并入比较键。
- **投票结果 XSS**：`renderInfo` 放逐/当选警长结果的玩家名未转义（昵称含 HTML 可注入），已补 `escapeHtml`；全量扫描确认这是唯一遗漏处。
- **夜晚过渡遮罩**：启用 CSS 中一直未使用的 `#overlay-night`——夜晚开始弹出"第 N 夜"全屏遮罩（含本人身份卡片，可点"开始行动"提前关闭，6 秒防呆自动关闭）；天亮时弹出"天亮了"过渡提示（2.5 秒）。
- **文档与代码对齐**：
  - `game.js` 狼美人技能描述修正（实际规则：**仅被放逐时带走被魅惑者**，被狼刀/毒杀/枪杀均不触发——与场景8测试一致；该描述会在游戏内展示给玩家）；
  - `rules.md` 三.6 条修正（原误写"被猎人射杀也触发"）；轮询方式（自适应 700ms/1600ms + 版本号）、房间闲置回收（TTL）等实现细节同步；
  - `README.md` 版本号、测试清单（8 个脚本）、目录结构、轮询说明同步；

### 涉及文件

`game.js`、`public/client.js`、`public/index.html`、`public/style.css`、`rules.md`、`README.md`、`package.json`

---

每轮迭代均通过完整回归：

- `test/simulate.js` —— 主流程 + 场景 1~9 + 专项场景（丘比特自连/第三方/倒计时/狼美人枪杀）
- `test/client-flow.js` —— 浏览器流程模拟
- `test/check-s1.js` —— 狼频道可见性专项
- `test/check-security.js` —— 安全加固专项（路径穿越/参数校验/413/TTL 实测）

运行：`cd test && node simulate.js`（Windows 下用 `..\node.exe simulate.js`）

---
## 🔒 1.0.0-rc.5 —— 安全加固

**此前的问题**：静态文件存在路径穿越漏洞（公网部署可读任意文件如 server.js / server.log / /etc/passwd）；房间无 TTL，玩家关掉标签页后房间永久残留（内存泄漏）。

### 修复内容

- **🔴 路径穿越漏洞（双层防御）**：解码后含 `..`（含 `%2e%2e`、`%2f`、`%5c` 等编码变体）直接 403；静态文件路径再校验必须位于 `public/` 目录内。实测 6 种攻击向量全部拦截。
- **🟡 房间 TTL**：房间创建时记录 `lastActive`，每次轮询刷新；超过 2 小时无活动的房间自动回收（可配置 `ROOM_TTL_MIN` / `ROOM_SWEEP_SEC` 环境变量）——关页面不留垃圾房间。
- **POST body 上限**：超 1MB 直接 413 并丢弃数据，防恶意大请求占内存。
- **参数校验**：`/api/state` 校验房间号（6 位）与玩家 ID（16 位 hex）格式。
- **日志轮转**：`server.log` 超 1MB 自动改名 `.old`。
- **EADDRINUSE 友好提示**：端口占用时给出明确中文提示。

### 涉及文件

`server.js`、`game.js`；新增安全测试 `test/check-security.js`

---
## 🎮 1.0.0-rc.4 —— 游戏体验细节

### 修复内容（5 项）

| # | 问题 | 修复 |
|---|---|---|
| S1 | 狼人频道历史白天"藏"在数据里但看不到 tab | 服务端狼频道改为仅夜晚可见（白天连历史也不下发），与 tab 消失行为一致 |
| S2 | PK 投票按钮列出所有存活玩家，点了非 PK 玩家被服务端拦截 | 按钮列表和玩家卡片点击都只允许平票玩家 |
| S3 | 夜晚"全体"频道输入框仍可用，发送报错 | 夜晚全体频道自动禁用输入框并提示"🌙 夜晚不能发言（狼人/情侣频道除外）" |
| S4 | 丘比特选完两人后无法更换 | 点已选玩家取消；两人已满时点第三人直接替换 |
| S5 | 没有狼美人时狼面板仍显示"魅惑目标"按钮组 | 队内无狼美人时整个魅惑区隐藏 |

### 涉及文件

`game.js`、`public/client.js`；新增回归测试 `test/check-s1.js`

---
## 🎨 1.0.0-rc.3 —— 界面与输入体验

**此前的问题**：UI 有些凌乱（勾选框变成大号方块、角色卡横向溢出、按钮文字换行高低不齐）；电脑端打字时遗言框经常被取消选中（内容丢失）。

### 修复内容

- **遗言框失焦修复**：渲染前快照正在编辑的输入框（内容+光标位置），渲染后自动恢复——无论别人怎么发消息、状态怎么刷新，遗言打字不再被打断（聊天框同样受益）。
- **复选框/单选钮样式修复（凌乱主因）**：全局 input 样式（边框、内边距、`width:100%`）不再套用到 checkbox/radio/slider，恢复为正常小控件。
- **UI 整理**：
  - 角色卡（身份选择）允许换行，不再横向溢出；
  - 按钮文字不换行，高度对齐；
  - 大厅规则行自动换行、间距加大；盗贼玩法长说明移到小字提示；
  - 顶栏窄屏自动换行、角色徽章超长省略；
  - 死亡公告原因右对齐；正文行高、小标题字距优化；
  - 移动端间距、玩家卡片、角色卡宽度适配。

### 涉及文件

`public/client.js`、`public/style.css`

---
## 🚀 1.0.0-rc.2 —— 稳定性与性能

**此前的问题**：公网（Cloudflare Tunnel）运行时经常卡顿、崩溃；房主设置里"警长选举"无法关闭（只能保持开启）。

### 修复内容

- **警长开关**：警长选举由单选按钮改为复选框，房主可自由开/关（关闭后跳过警长竞选、投票无 1.5 票）。
- **版本化轮询（关键优化）**：客户端轮询带上版本号 `?v=N`，服务端版本一致时只返回 `{"v":N,"changed":false}`（几十字节），不再重复计算和传输完整状态——隧道流量降低 90% 以上。
- **自适应轮询频率**：轮到玩家操作时 700ms 快轮询，等待他人/已出局时 1600ms 慢轮询（原为固定 600ms）。
- **gzip 压缩**：所有 JSON 响应和静态文件（client.js 46KB → 约 13KB）自动压缩。
- **崩溃容错**：`uncaughtException` / `unhandledRejection` 记录到 `server.log` 并保持服务器运行，不再一崩就整局中断；端口占用（EADDRINUSE）明确提示退出。
- **自动重启**：新增 `server-loop.bat`（node 崩溃 2 秒自动重启）；「开启公网联机.bat」重写——cloudflared 断线 3 秒自动重连、加 `--no-autoupdate`（cloudflared 自动更新是隧道断线常见原因）、node 未运行自动拉起。
- **运行统计**：每 10 分钟输出房间数/玩家数/内存，便于排查。

### 涉及文件

`public/client.js`、`server.js`、`开启公网联机.bat`、新增 `server-loop.bat`

---
## 📖 使用说明

1. **启动**：双击「开启公网联机.bat」（自动启动服务器+隧道，均有崩溃自动重启）；局域网玩双击「启动游戏.bat」。
2. **版本升级**：替换 `server.js`、`game.js`、`public/`、两个 bat、`package.json` 即可，无需安装依赖。
3. **浏览器**：升级后请 **Ctrl+F5 强制刷新**（否则加载旧版 client.js）。
4. **可调参数**（环境变量）：
   - `PORT`：端口（默认 3000）
   - `PHASE_TIMEOUT`：白天阶段倒计时秒数（默认 30）
   - `ROOM_TTL_MIN`：房间闲置回收分钟数（默认 120）
   - `ROOM_SWEEP_SEC`：回收扫描间隔秒数（默认 300）
## 📌 已知事项

- 服务器默认在 `data/rooms.json` 中保存房间快照（v1.5.6+；v1.6.4 起收纳到 `data/` 子目录），重启后自动恢复进行中的对局；如需完全清空，删除 `data/rooms.json` 即可（根目录旧文件启动时会提示手动移动）。Render 免费实例休眠后快照可恢复；但若平台回收磁盘，则快照亦会丢失；
- trycloudflare 免费隧道有带宽限速，长时间运营建议部署到 Render / Glitch / VPS；
- 同一账号开两个标签页：`view.v` 已兜住状态覆盖，服务端动作权威，基本无害。
## 
