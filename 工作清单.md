# 工作清单（下一版本）

> 状态：**收集阶段**
> 排期：**A 系列（A1–A4）= v1.6.4**；**B 系列（B1）= 1.7.0**；**C 系列（C1–C3）= 1.8.0**。
> 目标版本：v1.6.4（预计）。
> 背景：真实公网测试卡顿超时，cloudflared metrics 证实 trycloudflare 快速隧道 50% 请求失败（80/160 errors）、隧道注册 2.37s、边缘 lax01（洛杉矶）；服务器本身健康（connect latency 0、无 5xx）。

---

## 跨系列设计约束（三个闭环，已确认）

① **A2-4 → B1 → C1 是一条链**：B1 的 AdaBoost vote 上线后 bot 投票会更准，恰好加剧 A2-4"票得太准"抱怨——B1 纯决策强度**必须先到位**，C1 混沌层才有"偏离"的基准（混沌层先上线则 A/B 分不清"决策层弱"还是"混沌层拉低"）；落地顺序保持 B1 → C1
② **A2-5 模板池 = C1 的 lexicon.json，只做一套**：A2-5 直接按 lexicon.json schema（意图→语料库键值）做，C1 只消费它，避免两套文本系统并存
③ **A2-4 置信度来源最终只有一个：B1 的 Platt 概率**：A2-4 一期可用现有 suspicion 方差做轻量版，但接口从设计起就按"最终替换为 P(wolf|features)"——两套置信度系统并存是最大技术债

---

## A1 部分：公网稳定性三件套 + 可观测性（已确认，待实施）

### P1 —— 直接对抗 50% 失败率

- [x] **P1-1 POST 操作幂等重试**（`public/client.js` 的 `act()`）
  - `vote` / `wolf_set` / `guard_pick` / `dreamer_pick` / `seer_pick` / `witch_act` / `cupid_pick` / `confirm` / `handover` / `post` / `chat` 等幂等/可重复操作：失败自动重试 1~2 次（间隔 500ms）
  - 非幂等（`createRoom` / `joinRoom` / `kick` / `leave`）不盲重试，保持现状
  - 重试命中 50% 失败率时体感显著改善（"点不动"减少）
  - **opId 去重（必须，post/chat 不幂等）**：`post`（发言）/`chat`（聊天）重试时若第一次已到达服务端、仅响应丢失 → 会**重复发言**（真人局"一句话说两遍"穿帮，bot 局浪费发言配额）；服务端写操作统一入口加 opId 去重（`recentOps` Map：opId→result+ts，TTL 5min，上限 2000 条）；**result 只缓存轻量确认（P1⑥）**：`{ok:true, code:200, ts}`，**不缓存完整响应体**（大 result 如 state 快照会撑爆 Map，2000 条上限形同虚设；重试客户端收到"已确认"即视为成功）；**并发窗口（P0③ 落盘补录）**：开始处理时**先写入 pending 占位**（{status:'pending'}），处理完成更新为 {ok:true, code, ts}——重试命中 pending 时直接视为成功（写操作幂等）；若等处理完成才写 Map，请求 A 耗时 >500ms 时客户端已重试、B 到达时 Map 里还没有 A → 双执行（发言说两遍）；一个字段堵死窗口；**清理用懒清理（读取时比对 ts）+ O(n) 迭代删除最旧一半，不用定时器**；客户端每次操作生成 opId（`crypto.randomUUID()`）随 body 提交，**重试沿用同一 opId**；无 opId 的旧客户端直接放行；进程重启丢缓存可接受

- [x] **P1-2 失败风暴期轮询退避**（`public/client.js` 的 `poll()`）
  - 连续 N 次 poll 失败后，轮询间隔从 700/1600ms 自动拉长到 2s/4s（失败越多越慢）
  - 成功一次立即恢复原间隔
  - 目的：隧道最脆弱时不制造请求洪峰（越失败越重试、越重试越失败的恶性循环）

- [x] **P1-3 快速隧道自动禁用 SSE**（`public/client.js` 的 `connectSSE()`）
  - 检测页面 URL 为 `*.trycloudflare.com` 时直接纯轮询（`sseDisabled = true`），进房不再尝试 SSE
  - 省掉每次进房的 3 次 SSE 失败风暴（SSE 长连接是 http2 快速隧道被取消的主要对象）

### P2 —— 可观测性（下次不用靠猜）

- [x] **P2-1 服务器端指标**（`server.js`）
  - `/healthz` 或 `/api/stats` 新增：请求总数 / 失败数 / 平均延迟 / p95
  - 慢请求日志：>500ms 的 `/api/state`（可含全部 API）打一行 `server.log`
  - **注意 /api/stats 与 /api/debug 裸奔**：公网隧道下任何拿到 URL 的人都能看（stats=请求量/延迟；debug=房间事件流含玩家名，泄露更严重）→ token 校验（`STATS_TOKEN` / `DEBUG_TOKEN` 两个 env），或仅绑 localhost
  - **token 缺省行为（P2⑨）**：env 未设置时，端点**仅绑 localhost 可用**，公网访问直接 404/403——别让"忘了配 env"变成裸奔
  - **token 别放 query**：`/api/stats?token=xxx` 会被中间层 access log（Render/nginx）记录完整 URL → 用 `Authorization: Bearer` 或 `X-API-Token` header
  - **p95 别每次请求全量重算**：固定 bucket 直方图（毫秒粒度 ~20 bucket），p95 从直方图 O(1) 估算——轮询 700ms × N bot，别把统计本身做成热点
  - 慢请求日志加节流（≥1s 一条）
  - 目的：隧道与服务器谁慢一眼可见，不再依赖 cloudflared metrics 猜测

- [x] **P2-2 核对 headersTimeout ≥ keepAliveTimeout**（`server.js`）
  - **已确认是真 bug**：headersTimeout 默认 60s 且**计时包含 keep-alive 空闲期**（从上个响应结束就开始走表）→ 空闲连接 60s 即被销毁，keepAliveTimeout=65s 形同虚设 → cloudflared 反复重建连接放大失败数
  - 修复：`headersTimeout=75s > keepAliveTimeout=65s`；`requestTimeout=90s`（Node18+，接收完整请求含 body 上限，大 POST 防挂起）
  - 三者关系：requestTimeout(请求生命周期) > headersTimeout(含 keep-alive 空闲) > keepAliveTimeout(纯空闲)；修复后附注释

### P3 —— 弱网体验细节

- [x] **P3-1 SW 改 stale-while-revalidate**（`public/sw.js`）
  - 当前（v1.5.5）：非 2xx 回退缓存页
  - 升级：隧道失败时先用缓存渲染、后台静默刷新（先展示后更新），弱网下页面不白屏
  - 注意与 `/api/` 永不缓存策略的边界保持一致

---

## A2 部分：真实玩家反馈（讨论定稿，待实施）

> 来源：08-05 真实测试反馈。

- [x] **A2-1【严重-bug】全灭后游戏未正常结束**（"能全死，全死了还能继续"）
  - 待复现定性：①服务端 checkWin 漏判（某死亡链发生在 checkWin 之后，如猎人开枪/魅惑链角落）；②客户端在隧道 50% 失败率下未刷新到 ended（"看起来还能继续"）
  - 修复方向：复现脚本定位后修对应路径；客户端 ended 后强制立即刷新/弹终局
  - **终局幂等兜底（采用，触发点已修正）**：checkGameEnd() 保持幂等（已 ended 直接 return）；**两个兜底点**（都比"回合开始"更早）——①每次死亡/翻牌结算后立即调用（覆盖结算链中途全灭）；②每个阶段推进函数入口调用（覆盖"结算后无人可行动"的挂起场景）；原因：全灭若发生在结算链中间（猎人开枪→殉情→…），场上无活人时阶段推进可能挂起，永远到不了"下一回合开始"，兜底点必须前置
  - **兜底点必须在死亡链结算完全 drain 后（P1⑦）**：确认结算模型——当前 game.js 的死亡链（die → applyLoverChain → resolveShot/afterExile）为**同步递归**，兜底点放递归出口即可；若将来改异步/队列式，必须移至队列尾部（drain 回调），否则"结算中途"调用会**早判**（场上看似全灭，其实链条还没结算完）
- [x] **A2-2 狼人胜率过高**（13 人局狼全赢）：好人 bot 弱 + 好人不团结 → 狼轻松屠边。方向：配合 A2-3/A2-4 一并调整后重新跑平衡实验室验证胜率 ✅（v1.6.4 已验证：13人局狼胜率 100%→66.7%，仍偏高待 B1）
- [x] **A2-3 好人 bot 白天沉默**（"只有遗言会说话"/"预言家 p 都不放一个"）
  - 根因：平民 bot 主发言基本不发；easy 档预言家无"报查验"分支；被放逐/被踩时不辩解
  - 方向：idle 档保持沉默（挂机定位）；easy/smart/simulate 增加"质疑-回应"互动（被投/被踩开口辩解）；easy 预言家补报查验
- [x] **A2-4 投票失真（矛盾信号）**：13 人局 suspicion 漂移"全投神职"（投自己人）；小房间"票得太准没有昏招"
  - 方向：**不确定性表达**——置信度低时随机/跟风/情绪化犯错，高置信度才准（同时缓解"太傻"与"太准"两个抱怨）
  - **置信度接口先定死（P1⑧，跨系列约束③的落地形态）**：`confidenceOf(decisionBotId, targetId) => 0..1`——1.6.4 用 suspicion 方差实现，1.7.0 换 Platt 概率，**只换内部实现，调用方零改动**，两套置信度系统不共存
- [x] **A2-5 发言质量**：固定模板重复感（"聊天文本是固定的吗"）；纯气氛废话占比高（"说的话和游戏没关系"）
  - 方向：模板池扩容 + 组合式生成（前缀/核心/后缀 + 插入名字/查验结果）+ 气氛文案夹带游戏动作；暂不上 LLM；**按 lexicon.json schema（意图→语料库键值）做，C1 只消费**（见跨系列约束②）
- [ ] **A2-6 聊天功能与流程优化（"人斗人更刺激"）**：真人局体验（非 bot 问题）
  - 候选：发言轮次化（每人限时一轮）、警长竞选辩论轮、投票前最后陈词、聊天 UI（@引用/消息时间线）——**待用户明确期望**

## A3 部分：快照目录迁移（讨论中，待确认细节后实施）

- [x] **A3-1 快照收纳到新建子目录**（当前 rooms.json/rooms.json.bak/rooms.json.tmp 与 game.js 并列在根目录）
  - server.js：`SNAPSHOT_FILE` 改为 `path.join(__dirname, 'data', 'rooms.json')`；启动时 `mkdirSync(recursive)` 自动建目录
  - 同步：test/check-snapshot.js 的 SNAP 路径、.gitignore（data/ 或 data/rooms.json*）、check-docs 已知事项文案（"删除该文件"→明确 data/ 路径）
  - 待讨论：目录命名（data/ vs snapshots/）、是否保留旧路径自动迁移（建议不迁移，保持简单）、gitignore 粒度
  - **旧快照提示（P2⑩）**：不迁移 OK，但启动时检测根目录旧 rooms.json 存在 → 打一条 WARN 日志提示手动移动，否则用户"房间全没了"无解释

> 全部说完后：一次性实施 + 版本 bump + 更新公告 + 全量回归（含新增可观测性测试）。

## A4 部分：动态发言次数（已确认，待实施）

- [x] **A4-1 人机发言次数改为动态**：固定发言次数（当前每人每天 2 条）改为随**人类玩家比例**变化——人类越多，人机越不倾向发言
  - 待定参数：比例阈值/曲线（如人类占比 >50% 时 bot 减到 1 条、>80% 时仅在被质疑时开口等）
  - 影响点：bot-brain.js `talkedCount`/`markBotTalked` 上限、game.js `pendingBotActors` 的 `bt[p.id] < 2` 判定
  - **A4 别做深**：一期"被质疑时开口"用简单规则（被踩/被投时允许额外 1 条），C1 意图层上线后该规则自然被取代

## A5 部分：confidence.js 置信度接口（v1.6.4 建，P2 落盘补录）

- [x] **A5-1 新建 `server/ai/confidence.js`**：统一置信度入口，一期实现为 suspicion 方差版；B1 只替换内部实现为 Platt 概率；A2-4（不确定性表达）、C1 混沌层、B1 感知层三层共用同一入口——避免"两套置信度系统"（跨系列约束③；1.6.4 就建，比 1.7.0 再抽接口干净）
- [x] **A5-2 混沌层"有界"随信念差距动态调整**：信念集中（top1≈90%）只允许偏移到 top2；信念很散（前三 30/30/30）允许偏移 top3——直接消费 confidenceOf 输出，比固定"前 3 候选"更拟人，避免"上头投错"投出完全离谱的票（C1-1 执行层联动）

## B1 部分：人机强度系统升级（方案定稿：感知/规划分离，待实施）

> 架构（最新定稿）：**AdaBoost 管「信」（谁是狼）→ Rollout 管「谋」（我该怎么做）**，感知/规划分离（AlphaGo 路线小型化）。
> 与甲方案关系：甲方案"不做在线 MC 深验证"的顾虑（MC 评估器工程量大）被**层级自举**化解——Rollout 的快策略直接复用旧档（现 simulate/smart），无需新评估器。**本方案覆盖甲方案**。

- [ ] **B1-0 基线**：平衡实验室跑 500 局「旧 simulate vs 旧 simulate」建立基线胜率（≈50%）——定位：**冒烟 + 种子库建立 + 确定性验证**（不是验收，统计口径与 B1-6 分开）；B1-0 第一个验收项 = **同种子跑两遍，对局日志逐字节一致**（RNG 注入是否彻底的第一关）
- [ ] **B1-1 档位强度平移（层级自举，严格平移）**：新 simulate = Rollout(信念采样) + 快策略=现 simulate；新 smart = 现 simulate；新 easy = 现 smart（贝叶斯）——每层是上一层的 rollout policy；**easy 严格平移**：不保留旧启发式，完整继承现 smart 全部逻辑（含神职声称提取/穿衣服等）
  - **理由（已补充）**：平移之后还要叠加混沌层/意图层（C1：情绪化/犹豫，事件驱动），强度会较纯决策略有降低——严格平移保证最终各档强度不缩水（≈原档位强度），混沌层吃掉的强度正好由"高半档起步"对冲
  - **平移时同步完成 legacy/simulate 纯行动接口拆分**：拆出 `decideVote(world, state, rng)` / `decideNightKill(world, state, rng)` 纯策略函数（信念+公开状态 → 候选分布），rollout 只调用它——否则 rollout 调完整 simulate 仍带发言/态度逻辑，预算省不下来（B1-5 快策略落地的前提）
  - **态度必须排除干净（P0③）**：decideVote 定义为纯策略函数，**不含态度/情绪逻辑**（被踩报复性投票属于态度，由调用方在决策层之外叠加——正是 C1 执行层"有界覆盖"的位置）；否则 rollout 把态度卷进去 → 预算超支 + C1 混沌层无处安放
  - **decideVote 阵营分流（P0① 落盘补录）**：好人 `argmax P(wolf)`（投最像狼的）；狼 `argmin P(wolf)`（投最不像狼的=混淆视线的活人）**且排除狼队友**（与 C1-5② 同规则）——模型越准，狼 bot 用 argmax 越会投自己人；分流写进 decideVote 接口规格，B1-1 就落，别等 B1-3 训完再想
- [ ] **B1-2 训练数据管道**：check-balance-lab 挂样本采集钩子，输出对局日志（JSONL：玩家特征 + 真实身份 ground truth）；bot 接口暴露"全候选 + 特征"（现 createBotDecision 只给选中的 action）
  - **样本量现实（已确认）**：13 人局 night-kill 每局仅 1 个样本，500 局=500 条，AdaBoost 学不出东西（vote 一局 13 条则够）→ **一期只做 vote 一个决策头**；night-kill 用**间接方案**（按感知分数排序取 top + 旧 simulate 刀法，感知层同样收益、管道快速跑通）；night-kill 专用模型等 ≥3000 狼局再训；**night-kill 防自刀穿帮（P2 落盘补录）**：狼 bot 的感知分数是对好人的 P(wolf)——按 argmin 排序刀最低的好人，别刀出 P(wolf) 最高的队友
  - **vote 样本量上调（P1④）**：500 局 ≈6500 条对 20 特征 150 桩偏少（过拟合风险）→ 采集目标 **2000+ 局（≈2.6 万条）**；桩数上限随样本量调（2.6 万条时 150~200 轮合理）
  - **只采好人 vote 样本（P0① 落盘补录）**：狼 bot 的投票不进训练集——label 语义不同（狼的正确目标是好人），混入会污染模型；13 人局约 10 条/局
  - **训练/留出必须按 gameId 分层划分（P1④，防数据泄漏）**：同局样本高度相关，随机划分=泄漏；`splitByGame(seed)` 已内置
  - 类别不平衡（狼:好 ≈ 3:10）用 Platt scaling + 阈值校准兑住（B1-3 落实）
  - **vote 特征务必含位置类**（座位号/与发言者距离）——狼人杀信息量最高的公开特征之一（B1-7 ③ 落实）
- [ ] **B1-3 AdaBoost + Platt scaling**：决策树桩 ≈100 行；校准用**留出集**；离线训练 12p/6p 权重文件（models/adaboost-*.json，纯数据随仓分发、零依赖、可快照）；**一期只做 1 个决策头（vote）**，night-kill 走间接方案（见 B1-2），其余（查验/用药/守卫）二期复制同一流水线
  - **验收门槛（写死）**：留出集 Brier < 0.22（13 人局 3:10 先验的 Brier ≈0.21，模型必须明显好于先验）；且 avgPWolf - avgPGood > 0.2——否则先调特征/样本量，不达标不上线
  - **补第三件套 AUC（P1⑦ 落盘补录）**：留出集 **AUC > 0.6**（对类别不平衡稳健，直接对应"票得准"的玩家感知）；三件套：Brier（校准）+ AUC（判别）+ B1-6（端到端胜率），哪个不过都别上线——Brier 单看形同虚设（永远预测 30% 就几乎过线）
- [ ] **B1-4 感知层接入（两级注入，P0②）**：initBeliefs 是**开局一次性**先验，而 vote 特征（发言次数/被投票数/distance_to_me）局中才存在——单级注入逻辑不通 → ①**开局**：用静态特征（座位分布/人数配置）算 P(wolf) 进 initBeliefs；②**每轮投票决策前**：用动态特征算 P(wolf)，作为贝叶斯更新的**额外似然乘子**（与 check_wolf:19 乘子并列，一并重标定）；同时满足 A2-4 的"置信度唯一来源"约束；先在新 easy（现 smart）验证感知层单独价值
- [ ] **B1-5 规划层 Rollout**：信念采样 + 旧档当快策略；**决策预算（去歧义，P1⑤ 落盘补录）**：单 bot 单决策 ≤500ms 为目标值、2s 为硬上限（13 人局一轮投票 13 bot 串行 × 2s = 26s 会明显拖慢节奏；深度分层后单 rollout 毫秒级，30 个实际 <300ms，2s 只是保险丝）；rollout 数可配（默认 30~100），多 bot 串行；B1-0 基线局顺带测真实耗时
  - **采样（已修正）**：每个决策点独立采样（每决策 N 局 rollout、每局从 **bot 自己信念分布**采样一个世界，同局内各步共享、**不跨决策连续**——避免抽样误差被整局放大）；狼 bot 与好 bot 的采样世界彼此独立（各自只含自己知识，防好 bot 无意中沾到狼 bot 信息，守 B1-7 纪律①）
  - **预算控制（rollout 深度分层，最重要）**：不模拟到终局——白天投票决策只 rollout 到"本轮投票结束"（价值=谁被投出+身份揭示），夜间狼刀只到下一投票；单次 rollout 从秒级降到毫秒级，预算省一个数量级
  - **决策重要性自适应**：狼刀/关键投票/警长竞选 → 100 个 rollout；普通轮次 → 30 个
  - **快策略降级（预算最大头）**：rollout 内不跑发言/态度模型（经 B1-1 拆出的纯行动接口调用）；预算仍超再上 worker_threads 并行（CPU 密集，串行/并行是两回事，别指望 Promise.all）
- [ ] **B1-6 验收**：新 simulate vs 旧 simulate 自对弈，**交替换边/按阵营配对**（防阵营先验污染）；平衡实验室当裁判
  - **统计功效（已确认）**：N=200 检测不出 55%（p=0.55、N=200 → SE≈3.5%，95% CI ≈ 55%±6.9%，与 50% 无显著差异 z≈1.4）→ N=200 只当**冒烟**；正式验收 **N≥800**，或**配对设计**（固定随机种子、同一批种子局两边各跑一遍、逐局比较胜负——对局高度相关可大幅降方差，N=400 即可；与 S2 的 L2-lite 确定性重放联动）
  - **配对统计用 McNemar（P1⑤），不用配对 t**：胜负是二元结果，配对比较正确检验是 McNemar（2×2 表，χ²=(|a-b|-1)²/(a+b)），配对 t 是连续量工具；paired-eval.js 已实现
  - **胜率按阵营分别报**（好人胜率/狼人胜率）——交替换边只是平均掉了阵营效应，别让它掩盖信号
  - **配对种子必须打在显式 RNG 层**：rollout 采样随机、投票 tie-break、混沌层随机**全部注入显式 RNG（seedrandom 可注入），不能依赖全局 Math.random**——否则"同一种子"无意义（B1-8 联动）
- [ ] **B1-7 POMDP 纪律（坑）**：①Rollout 隐藏信息（未验身份/验人结果）一律从信念采样，绝不看真实角色；②**训练特征同样只含公开信息**（发言/投票/查验声明/位置），标签才用真实身份——特征层看真相同样是作弊；③先感知后规划（信念垃圾则 rollout 是"精心计算的错误"）；④离线训练 + 权重当数据发布（bot-train.js 离线 / 运行时加载，fail-open 回退旧档）
  - **特征构造只从 bot observe 接口（P1⑥ 落盘补录）**：禁止采集代码直接读 game.js state 把全知信息漏进特征（如未验身份）；采集与 rollout **共用同一套特征函数**——训练/推理特征天然同构，消灭"训练特征 A、上线特征 A'"漂移
- [x] **B1-8 显式 RNG 注入 —— 整个 B1 的第 0 步（P0①，前置）**：旧 simulate 当前含 Math.random（发言选择/tie-break/混沌随机），配对验收"同种子→同结果"的前提是引擎全部随机点走注入 RNG；**实施顺序排最前**，B1-0 的第一个验收项就是"同种子跑两遍，对局日志逐字节一致" ✅（v1.7.0 第 0 步完成，见文末实施记录）
  - server/ai/rng.js（零依赖 xorshift128+）：server.js 启动时 `global.rng = createRng(seed)`，所有 `Math.random()` 调用点逐个替换（替换范围限定**服务端** game.js/bot-brain.js；client.js 的 UI 随机不影响对局确定性，可保留）
  - 房间创建时从全局 RNG 派生房间种子；**快照记录 RNG 状态（s 数组）而非仅种子**（恢复后随机序列不重演，rollout 决策不回滚）
  - S2：Rollout 采样/决策随机都走注入 RNG，同时接进 L2-lite 动作日志，保确定性重放
  - **rollout 纯函数化（P0② 落盘补录）**：rollout 绝不 mutate 真实 game state——输入 world 快照 → 输出结果；若误碰全局 game.js 状态（哪怕只读全局变量），B1-0 的"同种子逐字节一致"会被微妙污染，并行后爆炸
  - **rollout 派生 RNG（P0② 落盘补录）**：rollout 内部用**派生 RNG 子序列**（每决策点从房间 RNG 派生），不直接消费房间 RNG——worker_threads 并行时多 worker 并发消费同一房间 RNG、顺序不定 → 确定性崩塌；聚合时**固定按 botId 顺序**汇总
  - S3：运行时成本控制（rollout 数可配、决策预算 ~2s、多 bot 串行）；模型版本与 check-version-sync.js 联动
- [ ] **B1-9 平衡验证**：升级后对比各档胜率/首刀分布/平均局时过线合入

> **实施顺序（修正版，P0① 前置 RNG）**：
> 0. B1-8 RNG 前置：显式 RNG 注入（全部随机点走注入 RNG）★新增第 0 步
> 1. B1-1 legacy/simulate 纯行动接口拆分（decideVote/decideNightKill，态度排除干净）
> 2. B1-0 基线：500 局 + 确定性验证（同种子两遍结果一致）+ 建立种子库
> 3. B1-2 样本管道：2000+ 局 vote 样本（按 gameId 记录，特征只含公开信息）
> 4. B1-3 AdaBoost + Platt：训练/校准/报告（Brier score 监控）
> 5. B1-4 感知层接入：两级注入（开局静态先验 + 每轮动态似然）
> 6. B1-5 Rollout：新 simulate（深度分层 + 预算 2s）
> 7. B1-1 阶梯重排：levels.js 严格平移映射
> 8. B1-6 配对验收：McNemar，N=400 配对 + 按阵营分别报
> 9. B1-9 平衡验证
> 每步独立验证、可回滚。

### B1 实施参考（代码增量已提供，落地时按此骨架）

- `tools/ai/train-vote-adaboost.js`：vote 决策头训练器（不平衡权重 + Platt + **按 gameId 分层留出** + Brier/校准报告；用法 `node tools/ai/train-vote-adaboost.js data/vote-samples.jsonl -o models/adaboost-vote-v1.json -t 150`；样本行含 gameId/roundIdx/botId/features/label，label 只用于训练、特征只含公开信息）
- `server/ai/rng.js`：显式可注入 RNG（零依赖 xorshift128+；`createRng(seed)` → next/int/pick/shuffle）
- `server/ai/rollout.js`：规划层骨架（深度分层 + 预算 30/100；信念采样/快策略模拟/tie-break 三处 TODO 接入点已标注；效用表按阵营×身份）
- `tools/ai/paired-eval.js`：配对验收（固定种子序列、旧/新同种子各跑一遍、McNemar χ²；前置：B1-8 RNG 落地）
- `server/ai/levels.js`：严格平移映射（easy←legacy/smart、smart←legacy/simulate、simulate←simulate-v2；回滚只需指回 legacy）
- 目录结构：`server/ai/legacy/{smart,simulate}.js`（拆分产物）+ `server/ai/simulate-v2.js`（新 simulate）
- **rollout 接入注意**：骨架中 `ctx.world` 持久化与 B1-5"每决策 N 局 rollout、每局采样一个新世界"需统一——每个决策点的 N 局 rollout 各自 `sampleWorldFromBelief(ctx, rng)`，world 只在**单次 rollout 内**各步共享（不跨 rollout 局、不跨决策点）

## C1 部分：新档位发言机制加强——混沌层 + 意图层（方案定稿，待实施）

> 定位：在 B1 严格平移之后叠加（增强发言机制），强度会较纯决策略有降低（已写入 B1-1 理由）。

- [ ] **C1-1 五段管线（正交分层）**：决策层（AdaBoost 信念 + Rollout）→ 意图层（发言动作）→ 混沌层（怎么偏离）→ 文本生成（语料库）→ 行为执行（vote/action 可被情绪覆盖）
  - 决策层：纯最优策略（训练目标）；**训练时不掺混沌**，否则模型会学成"爱生气"
  - 意图层：选择发言动作——报身份/踩/保/试探/悍跳/甩锅/带节奏等 5~8 种；由阵营 + 信念 + 态度 + 风格共同决定；**意图层需重写**（现有 botTalk 将整体大改，见 C2，不从现有 TALK_* 收敛）
  - 混沌层：情绪状态机 + 熵驱动犹豫 + 偶尔上头；**事件驱动，不随机驱动**
  - 文本层：每个意图动作一个语料库（成本大头，大量中文模板）；独立 JSON（lexicon.json）边玩边补
  - 执行层：投票/行动可被情绪覆盖，但**有界**（只在前 3 候选里上头，不是乱投）
- [ ] **C1-2 犹豫 ↔ 信息熵**：`entropy = -Σ p_i·log(p_i)`，信念越散 → 越犹豫（"我再想想…"/"不好说"）；确定性高的人机果断（"就是他，投"）——人类可读的拟人信号
- [ ] **C1-3 情绪化 ↔ 事件驱动**：直接消费 simulate 现有 5 态马尔可夫态度模型（attitudes）——被踩 → 愤怒值上升 → 回击倾向上升 → 偶尔"上头投错"；是"把已有状态接上表达层"，不是新系统
- [ ] **C1-4 核心原则：动机化错误，不是随机错误**：✅被踩 3 次 → 愤怒 → 抬杠/上头盯一个人 → 偶尔投错（人类可理解"他被激怒了"）；
- [ ] **C1-5 三个纪律**：①POMDP 纪律适用于情绪——混沌层只能由公开信息 + bot 自己信念驱动，绝不由隐藏信息驱动（狼 bot 不能因"队友被踩"莫名暴怒，那等于泄露阵营）；②混沌不破坏规则——情绪化可上头投错，但不能不刀人/不救人（行动完整性由决策层兜底，混沌只做有界偏离），**且上头范围排除狼队友**（防止情绪成为阵营泄露源，与纪律①③同一件事的另一面）；③阵营无差别——情绪/犹豫触发率好狼一致（防"bot 一犹豫就是狼"的免费外挂）
- [ ] **C1-6 风险与校准**：语料库是长尾成本（lexicon.json 独立维护）；可读性的度——混沌太弱没存在感、太强变搞笑角色；**用平衡实验室 A/B 量化"略有降低"**：新 simulate（无混沌）vs（有混沌）1000 局，目标"只降 ~4% 胜率但可读性翻倍"，用这个数字调混沌强度（可读性量化代理指标：**暂时搁置**；若想要廉价代理：统计混沌触发时 bot 发言的**情感词命中率**（lexicon 里标记情感词），承认不完美、不阻塞）

> 落地顺序：0 阶梯重排+纯决策训练（B1）→ 1 意图层重写（C2 讨论定稿后，5~8 种发言动作，先不接情绪）→ 2 混沌层（愤怒状态机 + 熵驱动犹豫，事件驱动）→ 3 语料库扩充（lexicon.json）→ 4 A/B 验证"略有降低"具体是多少，用数据调混沌强度

## C2 部分：现有 botTalk 发言体系整体大改（待讨论，方案未定）

- [ ] **C2-1 现有 botTalk（主/次发言、悍跳、施压、辩论、穿衣服、遗言、狼夜频道、气氛）需整体重设计**——与 C1 意图层重写联动；具体方案稍后讨论（先占位）

## C3 部分：人机模式与通用模式彻底分离（入口架构，方案定稿，待实施）

- [ ] **C3-1 起始页改双入口**：
  - **网络模式**：点进去才是「创建房间」和「进入房间」；人机陪练难度**保持原名**（挂机/简单/智能/模拟）
  - **离线模式**：无需房间名，点击进入**设置页**（不是直接开局）
- [ ] **C3-2 离线模式难度体系**：去掉「挂机」档；smart 难度重命名为 **normal**，simulate 重命名为 **hard**（内部 botLevel 值不改名，显示层映射 normal→smart、hard→simulate）
  - **easy 档保留**（离线模式共三档：简单 / normal（原 smart）/ hard（原 simulate））
  - **版本排期（防中间态）**：C3（入口分离 + 改名）整体放 1.8.0；1.7.0 只做 B1 内部平移（**名字不变**）——若 C3 先发，玩家选"normal"玩到旧 smart，1.7 平移后又变旧 simulate，同一名字两个强度，中间态混乱
- [ ] **C3-3 离线设置页（替代房主页，无房主）**：
  - 人数选择
  - 难度选择（normal/hard）
  - 屠边/屠城开关（winMode）
  - 警长竞选开关（settings.sheriff）
  - 身份增减选项（roleCounts 调整）
  - **作弊模式开关**（权限与房主特权一致，**去掉查看投票明细，其余都要**——即自选/代选身份 + 强制开始/推进）：打开后显示**自选身份开关**
  - 盗贼玩法开关（settings.thief）
- [ ] **C3-4 代码层分离**：离线建房走**独立路径**（不经过网络房间的房主流程），bot-brain 等按需分模块
  - **已确认形态**：仍连本机 server，房间带 offline 标记、跳过房主流程、玩家自动代行房主职责（复用现有引擎/快照/事件流）
  - **待确认（P2⑪）**：仍连本机 server 意味着**下载 zip 双击 index.html（file://）不可用**——目标形态是"起本地服务"（现状），还是需要纯静态打包（fetch 在 file:// 受限 + CORS，后者要改传输层）；影响 C3 架构，1.8.0 开工前定
  - **离线房间不写快照（P2 落盘补录）**：快照排除 offline 标记房间——否则 data/rooms.json（A3 迁完后）被一次性离线局污染，且离线局重启恢复无意义
- [ ] **C3-5**：作弊模式 = 房主特权 − 查看投票明细（即自选/代选身份 + 强制开始/推进）；离线局身份分配：默认随机，作弊模式下玩家可自选身份

---

## 版本排期（已确认）

| 版本 | 内容 | 理由 |
|---|---|---|
| **v1.6.4** | A1 + A2 + A3 + A4 | 止血（50% 失败率）+ 玩家反馈，改动小、可快速发布 |
| **1.7.0** | B1 全部（含 legacy/simulate 接口拆分） | 强度升级独立可验收（B1-0~B1-9 自带验收链）；**名字不变，无中间态** |
| **1.8.0** | C1（混沌/意图）+ C2（botTalk 大改）+ C3（入口分离 + 改名） | 混沌层依赖 B1 纯决策基线；入口分离与改名一次到位 |

---

## 自检节点（强制）

- [ ] **A 系列（A1–A4）完成后**：运行 `node tools/selfcheck.js --tests`（静态自检 + 全量回归）全面自检 → **发现问题先统计问题并输出清单（不直接改）→ 再修改 → 修改完再次全面自检 → 无问题后才进入 B 系列**
- [ ] **B 系列（B1）完成后**：同一循环（自检 → 统计输出 → 修改 → 再自检 → 无问题），通过后才进入 C 系列
- [ ] **C 系列（C1–C3）完成后**：同一循环，通过后才提交/发布
- 每次自检结果（通过项/失败项/测试数）记录在本文件末尾作为验收凭据


---

## 📋 v1.6.4 A 系列实施记录（2026-08-05，验收凭据）

- 实施范围：A1（全部）+ A2（A2-1~A2-5；A2-6 待用户明确期望未实施）+ A3 + A4 + A5
- 新增文件：`server/ai/confidence.js`（A5 置信度接口）、`server/ai/lexicon.json`（A2-5 语料库）、`tools/apply-changelog-164.js`（一次性公告更新脚本）
- 测试：新增 `check-opid.js`（11 断言）/ `check-game-end.js`（8 断言）/ `check-bot-expression.js`（4 组）/ `check-bot-vote-noise.js`（4 组）；更新 `check-snapshot.js`（data/ 路径）、`check-docs.js`（data/ 断言）；**全量 41/41 通过**；`tools/selfcheck.js --quick` 通过
- A2-2 平衡实验室（13 人局 smart×13，30 局）：**狼胜率 66.7%**（对比真实反馈“13 人局狼全赢”已显著改善）；首刀 100% 好人（狼 bot 首刀神职/高可信者策略生效）；好人胜 33.3%——仍高于理想（≈45-50%），留待 B1 强度系统 + 好人协作进一步平衡
- A2-1 修复根因：`checkWin` 对“无活人”返回 null → 改判平局（draw）+ 终局幂等 + 阶段入口 checkGameEnd 兜底；check-game-end 覆盖屠边/同归平局/无活人挂起/幂等
- 部署：替换 server/game/bot-brain/public（client.js+sw.js+index.html）/package.json，重启，Ctrl+F5


---

## 📋 v1.7.0 实施记录 · B1-8 显式 RNG 注入（第 0 步，2026-08-06）

- 新增 `server/ai/rng.js`：xorshift128+（零依赖）`createRng(seed, sArr?)` → next/int/pick/shuffle/state；`state()` 返回 [s0,s1]，快照存 s 数组、恢复后随机序列连续不重演
- **注入方式**：server.js 启动 `global.rng = createRng(SEED env 或随机)`；房间创建时 `room.rng = createRng(global.rng.int())`（从全局派生，房间间随机流独立）；game.js/bot-brain.js 全部 18 处 `Math.random` 替换完毕（game.js 6 处：randInt/shuffle 改签名 `(room, n/arr)`、newRoomCode/botDelay 走全局 RNG；bot-brain 16 处概率判断走 `rng().next()` + `CUR_RNG` 模块级当前决策 RNG，createBotDecision 入口设置——同步决策安全）
- **快照续流**：saveSnapshot 存 `rngState`（s 数组），restoreRoomFromSnapshot/loadSnapshot 重建房间 RNG（`createRng(0, sArr)`）——恢复后不重演
- **L2-lite 动作日志**：applyAction 成功时记 `room.actionLog`（{n, phase, step, actor: seat, action, data}，5000 条上限，不进 view，mood 不记，新局清空）
- **确定性验证工具** `tools/ai/determinism-check.js`：同种子跑两遍全 bot 对局（驱动消除调度竞态：bot 行动完成再推进、discuss 等发言达配额、vote 等 bot 投完），actionLog 归一化（player id → 座位号）后逐字节对比
- **验收结果**：seed 42/7/99/1234/2026 × cap 6/8/10/13 × easy/smart/simulate 全部**两遍一致**（27~95 条动作逐字节一致）；全量 41 测试绿
- 顺带修复：check-bot-opt O2 暴露的"卖狼策略被 A2-4 投票波动覆盖"（卖狼/被查杀目标豁免波动，v1.6.4 遗留 40% 概率误投）

### 📊 蒙特卡洛实验室平台落地（test/lab/，数据生产/消费分离）

- **GameRecord schema（lab.game-record@1）**：gameId/seed/scenario/config/result{winner,timeout,error{kind}}/players/events/firstKill——跑局与分析解耦，胜率报告/训练样本/配对统计/确定性重放共用同一数据
- **core/**：config（preset<CLI+校验+默认 botLine）、room-runner（确定性驱动：等 bot 行动/投票/发言达配额再推进，消除调度竞态；错误分类 config/engine/stall；B1-8 seed 注入）、pool（并发+seed 派生+checkpoint 续跑）、recorder（流式落盘+gameId 去重）、events（映射表按 game.js 核对固化）
- **scenarios/**：baseline（Wilson CI 胜率+首刀分布）、sample（B1-2 样本管道 JSONL）、deterministic（B1-0：同种子两遍事件流 hash，id→座位归一化后对比）、paired（B1-6：同种子双策略 McNemar）
- **stats/**：wilson/mcnemar（含连续性校正+erfc）/report——纯函数，check-lab-stats 单测覆盖
- 依赖方向（单向）：scenario → core → game.js；core 不 import stats
- 验证：deterministic 同种子两遍 hash 一致（3/3，B1-8 生效）；smoke/baseline/sample/paired 全部跑通；lab 快节奏（BOT_DELAY_MS=100，仅影响调度时序不影响决策序列）
- 数据落 data/lab-*.jsonl（gitignore）；任何新实验 = 新增 30 行 scenario，core 零改动

### 🚀 1.7.0（B1）实施记录——感知层 AdaBoost + 规划层 Rollout
- **B1-8 显式 RNG**：server/ai/rng.js（xorshift128+）全局+房间级；快照续流；determinism-check 同种子逐字节一致；rollout/decide 平局均注入 rng
- **B1-1 纯行动接口**：server/ai/legacy/decide.js（decideVote/decideNightKill，阵营分流/跟票/卖狼/恋人/波动分层）
- **B1-0 基线**：simulate 档 13 人局狼 96.2%（500 局 records 落盘 data/b1-baseline.jsonl）
- **B1-2 样本管道**：features.js 13 维公开特征（位置/发言/查杀/金水/票型/行为一致性）；game.js 决策时刻采集钩子；2000 局 56757 条样本（data/vote-samples.jsonl）
- **B1-3 AdaBoost+Platt**：200 桩；三件套验收 Brier0.0301/AUC0.9901/avgP差0.7799 全过；models/adaboost-vote-v1.json；修复训练器两处公式方向 bug（桩错误公式、AUC 排序）
- **B1-4 感知层**：model-loader fail-open；好人侧投票前 0.6信念+0.4模型混合；同 seed 100 局好人胜率 3%→40%
- **B1-5 Rollout**：信念采样+模拟本轮投票；逐候选"投X促成X放逐"收益（+2/-1）；64 世界；修复：搭便车/风险规避/采样噪声（worlds 12→64）
- **B1-1② 阶梯重排**：LEVEL_MAP（easy←现smart、smart←现simulate、simulate←新simulate）；修复狼恋人辩护不可达（悍跳 return null 拦截）、beliefs 缺失两处崩溃（狼队共享/calibrateBeliefs）
- **B1-6 配对验收**：lab paired 400 局，新simulate vs 旧simulate discordant 83:56，χ²=4.86 p=0.0274 **显著通过**
- **B1-9 平衡验证**：13 人局狼 easy63%/smart84%/simulate83%；模型对好人侧提升最大；较基线改善未均衡（待后续：好人协作/更多神职/真预言家强度）
- **实验室平台增强**：--bots、baseline --out、sample --sample-file、LAB_NO_MODEL、config 驼峰、默认 botLine；全量 42 测试绿

---

## 📋 v1.7.1 实施记录（2026-08-05）——可注入时钟 + 虚拟时间加速
- 目的：全量回归小时级→分钟级、样本管道分钟级→秒级、规则测试毫秒级；同 seed 双跑严格确定
- ①server/clock.js（real/virtual 双模式）②game.js 37 处换 clock.*（bot-brain 盲区自查通过）③debugRoom 摆盘构造器 ④lab 虚拟时间驱动（runOneLabGame+seedHash）⑤守卫 check-lab-virtual（实测 50 局 0.52s、43/43 全量通过）
