# 依赖与构建（本仓库零依赖，无需 npm install，node_modules 可忽略）
node_modules/

# 压缩包与备份
*.zip
*.tar.gz
*.7z

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# 环境变量与密钥
.env
.env.*

# 日志
*.log
logs/
server.log.old

# 本地运行文件（体积大，不入库）
node.exe
cloudflared.exe

# 房间快照（含玩家 ID 与身份，隐私数据，绝不上库）
# v1.6.4（A3）：快照收纳到 data/ 子目录（根目录旧文件仅启动时 WARN 提示，不自动迁移）
rooms.json
rooms.json.bak
rooms.json.tmp
data/

# AI 实验模型档（不入库；只提交推理端消费的模型：value-hicvn-v42 / value-vote-v31 / adaboost-vote-v2 等）
# 训练/评估中间产物：v1/v41-* 系列、frac/mlp/smoke/noinfo、*-audit 报告档——留本地防误删，重训可重建
models/value-hicvn-v1.json
models/value-hicvn-v1-audit.json
models/value-hicvn-v41-*.json
models/value-hicvn-v42-audit.json
models/value-hicvn-v42-noinfo*.json
models/value-hicvn-frac.json
models/value-hicvn-mlp.json
models/value-hicvn-smoke.json

# 内部工作清单（不公开）
工作清单.md

# 跑批输出（统一在 data/batch——test/lab/data 仅保留必要小文件）
test/lab/data/

# BGM 音频素材（QQ音乐录音转码产物——见 tools/music/convert-wav.js 重新生成）
public/music/

# 隧道运行时文件
tunnel.pid

# QQ音乐 hook 运行日志（不入库）
tools/qqmusic-dump/*.log
