fix+refactor: code review round - B1 role popup skill desc always empty (use shared SKILL_TEXT); B2 null-guard 5 init listeners (defensive); B3 quick-phrase onclick injection (JSON.stringify+escapeHtml); C1 #info/#panel double-sticky overlap (only #info sticky); C2 card-back glyph U+1F0A0 missing on Windows (CSS star pattern) + add player name to flip front; C3 chat popIn only on last message; S1 death toast own color type; S2 hotkey 0=seat10; S3 force-continue lights up in reveal; S5 cancel stale panel swap animation; S6 pick-sweep only with alive targets; remove dead keyframes ovFade/pulse and unused .toast-ok